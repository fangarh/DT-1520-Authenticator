# Session Log 2026-04-14

## Entries

- Создан `vault-first` протокол работы с проектом.
- Добавлены верхнеуровневые заметки `Start Here`, `Current State`, `Decision Index`, `Open Questions`, `Implementation Map`.
- Зафиксировано, что `OTP/` является рабочей точкой истины для решений и проектного контекста.
- Ранее созданные заметки по `2FA/MFA` оставлены в `OTP/2FA/` и включены в общий маршрут чтения.
- `OTP/Architecture/` подготовлена как целевая зона нормализации, но перенос заметок туда отложен из-за файловой блокировки.
- Созданы канонические доменные ветки `Architecture`, `Data`, `Integrations`, `Product`, `Delivery`.
- Добавлены `ERD`, черновой `OpenAPI v1` и план реализации на `8-12` недель.
- `OTP/2FA/` переведена в режим исторического слепка, а не основной точки чтения.
- `openapi-v1.yaml` усилен: добавлены `securitySchemes`, единая ошибка `Problem`, top-level `webhooks`, `Idempotency-Key` и более строгие схемы ресурсов.
- Добавлены `OAuth 2.0` token flow для интеграционных клиентов, refresh flow для device tokens, operation-level callbacks, response headers и отдельный `ADR-006`.
- `openapi-v1.yaml` прогнан через `Redocly CLI`; после исправлений callback/webhook security, operationId, license и headers спецификация валидна без предупреждений.
- Утвержден baseline: `ASP.NET Core`, `PostgreSQL`, `Redis`, `Outbox + Worker`, `React + Vite`, `Kotlin Android`, observability stack на `OpenTelemetry + Prometheus + Grafana + Loki + Tempo`.
- `iPhone` выведен из обязательного `MVP` и оставлен как growth track.
- Добавлена deployment-схема `MVP`.
- Зафиксированы bootstrap-параметры Android-проекта: `DT 1520 Authenticator`, `ru.dt1520.security.authenticator`, `mobile`, `Min SDK 28`, `Empty Activity`, `Compose`, `Kotlin DSL`.
- Созданы scaffold-проекты: `mobile`, `backend`, `admin`, `infra`.
- Для `admin` создан `React + Vite` scaffold, установлен `npm install`, build проходит.
- Для `backend` создан `ASP.NET Core` scaffold с solution и модулями; restore/build успешно выполнены после запуска вне sandbox.
- Добавлены локальные примеры `MCP`-конфигов в `config/mcp`.
- Принято решение отказаться от верхнего `src/`; канонические корни переключены на `backend`, `mobile`, `admin`.
- Выполнен cleanup: удалены старый `src/`, внутренний `backend/src/`, generated-cache директории и локальные IDE/build хвосты.
- После переноса workspace повторно проверены зависимости: `admin` снова проходит `npm install` и `npm run build`, `backend` проходит `dotnet restore`, `mobile` резолвит Gradle-зависимости при корректном `JAVA_HOME`.
- Для `backend` добавлен `Directory.Solution.props` с отключением параллельной solution-сборки, чтобы убрать нестабильные file locks в `artifacts/obj`.
- Обновлены `README` в корне и в рабочих корнях `admin/backend/mobile` с актуальными командами для этой папки.
- После проверки удалены локальные verification/build артефакты из `admin`, `backend` и `mobile`, чтобы не оставлять generated хвосты в workspace.
- Добавлена каноническая ветка `OTP/Security/` и индекс для security-заметок.
- Подготовлены draft-документы `Backend Module Design`, `Security Model MVP`, `Testing Strategy`, `Documentation Backlog` и `Week 1 Execution Plan`.
- В `Current State`, `Start Here`, `Implementation Map`, `Architecture Index` и `Delivery Index` добавлены ссылки на новый пакет planning/security заметок.
- Приняты `ADR-011`, `ADR-012` и `ADR-013`: `single-tenant deployment` при tenant-aware модели, `admin-led enrollment` для `MVP`, trust lifecycle устройства без mandatory attestation в `v1`.
- Приняты `ADR-014` и `ADR-015`: `2FA Server` остается `MVP`, но продукт стратегически идет к `IdP`; `push` не является обязательной опорой для `on-prem` и future air-gapped профилей.
- Принят `ADR-016`: `Policy` обязателен уже в `MVP`, но реализуется как внутренний модуль монолита с code-first правилами и `deny by default`.
- Добавлен `Policy Design` как канонический draft-контракт для `PolicyContext`, `PolicyDecision`, `MVP` ruleset и backend mapping.
- В backend добавлены `Policy` enums, contracts, `DefaultPolicyEvaluator`, регистрация в `Api` и unit tests в `OtpAuth.Infrastructure.Tests`.
- Проверка после фиксов: `OtpAuth.Infrastructure.Tests` проходит `dotnet test`, `OtpAuth.Api` проходит `dotnet build`; для этого понадобился запуск вне sandbox из-за file-lock проблемы в `artifacts/obj`.
- Добавлен `backend/scripts/verify-backend.ps1` и обновлены инструкции: backend-проверку нужно запускать последовательно, без параллельного `build/test`.
- Реализован первый `CreateChallenge` slice: domain `Challenge`, application handler, in-memory repository, `POST /api/v1/challenges`, mapper/response model и unit tests для handler.
- Первый `Challenge` slice доведен до read path: добавлены `GetChallengeHandler`, `GET /api/v1/challenges/{id}` и unit tests для кейсов `found/not found/invalid id`.
- Последовательная backend-проверка через `backend/scripts/verify-backend.ps1` после добавления retrieval-path проходит успешно: `12/12` тестов, solution build без ошибок.
- Добавлен `VerifyTotp` flow: `VerifyTotpHandler`, `POST /api/v1/challenges/{id}/verify-totp`, bootstrap `TOTP` verifier и state transitions `pending -> approved/failed/expired`.
- По security для bootstrap flow зафиксировано: невалидный код закрывает challenge в `failed`, verifier использует process-local master secret и не считается production-ready заменой enrollment-backed secret storage.
- Последовательная backend-проверка после добавления `VerifyTotp` проходит успешно: `18/18` тестов, solution build без ошибок.
- Принят `ADR-017`: перед добавлением нового `enum` в проекте нужно проверять, не лучше ли выражать поведение через отдельные типы, стратегии или иерархию объектов.
- Проведена ревизия текущих `enum` в backend: немедленного рефакторинга не требуется; `FactorType` отмечен как зона наблюдения, если factor-specific behavior начнет расползаться за пределы `Policy` и `Factor Engine`.
- Для `Challenges` реализован bootstrap integration auth layer: bearer token аутентифицирует integration client context, а handlers требуют `challenges:read/write` и проводят tenant/application scoping.
- В `InMemoryChallengeRepository` добавлен scoped lookup, чтобы чтение и verify чужого `challengeId` возвращали `404` вместо утечки информации о ресурсе.
- `backend/scripts/verify-backend.ps1` после добавления auth/scoping проходит успешно: `26/26` тестов, solution build без ошибок.
- Dev-only bearer auth заменен на bootstrap `OAuth 2.0 client_credentials`: добавлены `/oauth2/token`, bootstrap in-memory client store, `client_secret` hash verification, JWT issuance и `JwtBearer` validation.
- `Challenges` endpoints переведены на standard auth pipeline с claims-based integration client context и policy-level scope checks.
- По security зафиксировано bootstrap-ограничение: client registry пока in-memory, token revocation/introspection отсутствуют, а без `BootstrapOAuth__SigningKey` используется process-local signing key.
- Последовательная backend-проверка после добавления OAuth проходит успешно: `33/33` тестов, solution build без ошибок.
- Принят `ADR-018`: в backend не использовать `Entity Framework`; persistence baseline проекта фиксируется как `Dapper + free mapper`.
- Для коммерческого использования как конкретный mapper зафиксирован `Mapperly` (`Apache-2.0`, source generator, без runtime reflection).
- Принят `ADR-019`: схемой `PostgreSQL` управляет отдельный `FluentMigrator` runner, а не `Api` и не `EF`.
- В backend добавлены `OtpAuth.Migrations`, initial migration для `auth.integration_clients` и `auth.integration_client_scopes`, helper для `ensure-database` и bootstrap script `backend/scripts/initialize-postgres.ps1`.
- Bootstrap `OAuth` переведен с in-memory client registry на `PostgreSQL`-backed `PostgresIntegrationClientStore` на `Dapper + Mapperly`.
- На предоставленном сервере `PostgreSQL` создана база `dt-auth` и применена начальная схема без создания новых DB users.
- Unit test suite расширен до `38/38`, включая покрытие connection string helper и bootstrap seed material factory.
- Bootstrap integration client seeded в `dt-auth` через explicit seed-команду без сохранения секрета в репозитории.
- `/oauth2/token` успешно проверен end-to-end против локально поднятого `Api`, подключенного к реальному `dt-auth`.
- `Challenges` переведены с `InMemoryChallengeRepository` на `PostgreSQL`-backed `PostgresChallengeRepository` на `Dapper + Mapperly`.
- Добавлена миграция `auth.challenges`, unit tests расширены до `44/44`, включая `ChallengeDataMapper` и bootstrap `TOTP` secret provider.
- Для dev/bootstrap добавлен env-based `BootstrapTotp__MasterSecret`, чтобы внешний `verify-totp` flow можно было воспроизводимо проверить без записи секрета в repo.
- Flow `CreateChallenge -> GetChallenge -> VerifyTotp` успешно проверен end-to-end через локально поднятый `Api`, bootstrap OAuth и реальный `dt-auth`.
- Добавлены миграции `auth.totp_enrollments` и `auth.challenge_attempts`.
- `VerifyTotpHandler` теперь пишет append-only `challenge_attempts` и использует enrollment-backed `PostgresTotpVerifier`.
- Добавлены `TotpSecretProtector`, `PostgresTotpEnrollmentStore`, `PostgresTotpEnrollmentSeeder` и bootstrap seed-команда для `TOTP` enrollment.
- `TOTP` secret теперь хранится в `PostgreSQL` только в зашифрованном виде через env-managed protection key; это зафиксировано как bootstrap-stage до `Vault/KMS`.
- Backend-проверка после этого шага проходит с `52/52`, а enrollment-backed flow `CreateChallenge -> GetChallenge -> VerifyTotp -> GetChallenge` успешно подтвержден end-to-end против `dt-auth`.
- Для `VerifyTotp` добавлены persistent anti-replay reservations в `auth.totp_used_time_steps` и runtime rate limiting на основе `auth.challenge_attempts`.
- `VerifyTotp` теперь возвращает `429` с `Retry-After`, а replay маскируется наружу как generic invalid one-time password без раскрытия внутренних счетчиков и статусов.
- Unit tests переписаны под новый verifier contract и расширены до `59/59`, включая `rate limited`, `replay detected` и threshold decision tests.
- `initialize-postgres.ps1` стабилизирован: сначала делает последовательную сборку migration runner-а, затем выполняет команды через `--no-build`.
- В runtime хосте `OtpAuth.Api` отключена зависимость от Windows `EventLog`; локальный bootstrap API больше не падает на первом запросе из-за logging permissions.
- Против реального `dt-auth` подтвержден end-to-end сценарий: первый `TOTP` approve успешен, повтор на том же time step получает `422`, серия неверных verify-операций приводит к `429`.
- Добавлены bootstrap `OAuth` endpoints `POST /oauth2/introspect` и `POST /oauth2/revoke`.
- Добавлен persistent store `auth.revoked_integration_access_tokens` и runtime-проверка revoked access token на auth boundary через `JwtBearer` events.
- Introspection и revoke требуют `client_id + client_secret`; чужой токен не раскрывается, а revoke для него превращается в безопасный no-op.
- Unit tests расширены до `71/71`, включая credential validation reuse, JWT introspection, token revocation handlers и runtime validation revoked tokens.
- Против реального `dt-auth` подтвержден сценарий `token -> introspect(active=true) -> revoke -> introspect(active=false) -> protected endpoint returns 401`.
- `TotpSecretProtector` переведен на rotation-ready key ring: current key используется для новых writes, legacy keys доступны для decrypt по `key_version`.
- Bootstrap JWT issuer переведен на current + legacy signing keys; новые токены получают `kid`, а introspection/validation умеют принимать токены, подписанные legacy key.
- Unit tests расширены до `75/75`, включая legacy `TOTP` key decrypt, JWT `kid` emission и validation legacy-signed token после ротации.
- Runtime against real `dt-auth` подтвержден: existing enrollment, зашифрованный ключом `v1`, успешно проходит `VerifyTotp` при current key `v2` и подключенном legacy key `v1`.
- Добавлены maintenance workflow для security-данных: `reencrypt-totp-secrets` и `cleanup-security-data` в `OtpAuth.Migrations`, плюс wrapper `backend/scripts/maintain-security-data.ps1`.
- Реализованы `TotpSecretsReEncryptionService` и `SecurityDataCleanupService`; cleanup охватывает `challenge_attempts`, `totp_used_time_steps` и `revoked_integration_access_tokens`.
- Unit tests расширены до `79/79`, включая re-encryption и cleanup/retention сценарии.
- Против реального `dt-auth` выполнен `TOTP` re-encryption workflow: `Scanned=1, ReEncrypted=1, Skipped=0`.
- Против реального `dt-auth` выполнен cleanup security-данных: удалены `2` истекшие anti-replay reservations, остальные категории на момент запуска не требовали очистки.
- Runtime verify после re-encryption повторно подтвержден уже без legacy `TOTP` key в конфигурации: enrollment, ранее зашифрованный ключом `v1`, успешно проходит `VerifyTotp` только с current key `v2`.
- Принят `ADR-020`: lifecycle integration clients должен инвалидировать уже выданные JWT через persisted auth-state timestamp, а не только запрещать новую выдачу токенов.
- Для `auth.integration_clients` добавлена миграция `202604140006_AddIntegrationClientAuthStateTracking` с полем `last_auth_state_changed_utc`; миграция применена к реальному `dt-auth`.
- Реализованы `PostgresIntegrationClientLifecycleStore`, `IntegrationClientLifecycleService` и wrapper `backend/scripts/manage-integration-client.ps1` для rotate secret и deactivate/reactivate.
- JWT integration access token теперь выпускается с `iat`, а runtime validation и introspection проверяют `iat >= last_auth_state_changed_utc`.
- Unit tests расширены до `86/86`, включая integration client lifecycle service, `iat` claim и invalidation токена после client auth state change.
- Для legacy signing keys добавлен lifecycle через `RetireAtUtc`: runtime/introspection больше не принимают retired `kid`, а key resolver работает по `fail-closed`, если `kid` присутствует, но активного key уже нет.
- Добавлен `BootstrapSigningKeyRing`, отдельная inspection-команда `inspect-signing-key-lifecycle` и wrapper `backend/scripts/inspect-signing-key-lifecycle.ps1` с operational guidance по rollout/retirement.
- По security ужесточено правило: process-local ephemeral signing key допустим только в `Development`; вне него `Api` требует явный `BootstrapOAuth__CurrentSigningKey`.
- Backend-проверка после signing key lifecycle изменений проходит успешно: `89/89` тестов, включая retirement legacy signing key и inspection workflow.
- Для signing key lifecycle добавлен append-only audit trail: миграция `auth.signing_key_audit_events`, `SigningKeyLifecycleAuditService`, команда `audit-signing-key-lifecycle` и reporting-команда `list-signing-key-lifecycle-audit-events`.
- Audit snapshot сериализует только sanitized metadata key ring-а; signing material в `payload_json` не сохраняется.
- Backend-проверка после добавления signing key audit/reporting проходит успешно: `92/92` тестов.
- Для `TOTP` protection key lifecycle добавлены inspection/audit/reporting: миграция `auth.totp_protection_key_audit_events`, report по usage `key_version`, команды `inspect-totp-protection-key-lifecycle`, `audit-totp-protection-key-lifecycle`, `list-totp-protection-key-lifecycle-audit-events`.
- Audit snapshot по `TOTP` protection keys сериализует только sanitized metadata (`key_version`, backlog, warnings) и не сохраняет key bytes или ciphertext.
- На реальном `dt-auth` применены миграции с `SSL Mode=Disable`; inspection подтвердил `current key version = 2`, `reencryption backlog = 0`, а первый audit snapshot успешно записан и читается через reporting.
- Backend-проверка после добавления `TOTP` protection key audit/reporting проходит успешно: `94/94` тестов.
- Добавлен unified security audit contour: generic `SecurityAuditService`, таблица `auth.security_audit_events` и backfill существующих signing/`TOTP` lifecycle snapshot-ов в единый append-only trail.
- Lifecycle integration clients теперь тоже пишет sanitized audit events; проверка на реальном `dt-auth` подтверждена безопасным no-op `activate-integration-client otpauth-crm`, который записал `integration_client_lifecycle.state_change_skipped`.
- На реальном `dt-auth` unified audit trail подтвержден командами `migrate`, `audit-signing-key-lifecycle` и `list-security-audit-events`; в trail видны `signing_key_lifecycle.snapshot`, `totp_protection_key_lifecycle.snapshot` и `integration_client_lifecycle.state_change_skipped`.
- Migration runner исправлен для запуска из корня репозитория: `ResolveApiProjectPath()` теперь находит `backend/OtpAuth.Api`, а backend-проверка после изменений проходит с `99/99` тестов.
- Реализован `TOTP` enrollment vertical slice: `POST /api/v1/enrollments/totp` выдает provisioning artifact, а `POST /api/v1/enrollments/totp/{id}/confirm` подтверждает enrollment через код из authenticator.
- Для confirm-path добавлена persisted brute-force защита через `auth.totp_enrollments.failed_confirm_attempts`; после `5` неудачных попыток enrollment блокируется до нового start.
- Unified security audit trail расширен на enrollment lifecycle: `totp_enrollment.started`, `totp_enrollment.confirmed`, `totp_enrollment.confirmation_failed` и `totp_enrollment.confirmation_locked`.
- Последовательная backend-проверка после enrollment slice проходит успешно: `110/110` тестов, solution build без ошибок.
- Зафиксированы readiness gates для старта `admin UI` и `Android`: `admin` начинается после завершения backend lifecycle `start/confirm/revoke/replace/status`, а `Android` можно начинать только как `TOTP-first` slice до готовности device lifecycle и `push`.
- Зафиксирован iterative plan `TOTP Enrollment Management Iterations`; первая итерация завершена через `GET /api/v1/enrollments/totp/{id}`.
- Read path для enrollment возвращает только scoped status и не повторяет `secretUri`/`qrCodePayload`.
- Последовательная backend-проверка после Iteration 1 проходит успешно: `113/113` тестов, solution build без ошибок.
- Завершена Iteration 2 `TOTP enrollment revoke`: `POST /api/v1/enrollments/totp/{id}/revoke` выполняет фактическую деактивацию enrollment-а через `is_active = false`.
- Enrollment lifecycle audit расширен событием `totp_enrollment.revoked`; revoke не является UI-only действием и сразу исключает enrollment из active verify-path.
- Последовательная backend-проверка после Iteration 2 проходит успешно: `117/117` тестов, solution build без ошибок.
- Завершена Iteration 3 `TOTP enrollment replace`: `POST /api/v1/enrollments/totp/{id}/replace` создает pending replacement artifact, не отключая текущий активный фактор до успешного confirm.
- В `auth.totp_enrollments` добавлены отдельные replacement-колонки и `replacement_failed_confirm_attempts`; replacement brute-force не влияет на текущий active verify-path.
- Enrollment lifecycle audit расширен replacement-событиями `replacement_started`, `replacement_confirmed`, `replacement_confirmation_failed` и `replacement_locked`.
- Последовательная backend-проверка после Iteration 3 проходит успешно: `123/123` тестов, solution build без ошибок.
- Завершена Iteration 4 `TOTP enrollment endpoint hardening`: добавлен `WebApplicationFactory`-based API harness с test auth и in-memory provisioning store.
- Enrollment API теперь покрыт endpoint-level сценариями `401/403/409`, scope enforcement, `Location` header на start flow, secret non-leakage вне `start/replace` и safe replacement confirm-path.
- Последовательная backend-проверка после Iteration 4 проходит успешно: `133/133` тестов, solution build без ошибок.
- Принят `ADR-025`: `bootstrap/setup plane` отделяется от runtime `Admin UI`; админка не получает host-level прав на запуск backend-а, а delivery-порядок зафиксирован как `setup plane design -> Admin UI -> Installer MVP`.
- Добавлен `OTP/Delivery/Admin UI MVP Plan.md`: зафиксированы scope, экраны, frontend module layout, тестовый контур и security gates для runtime `TOTP` enrollment management.
- Приняты `ADR-026` и `ADR-027`: browser `Admin UI` не использует integration `client_credentials`, а operator lookup строится по current enrollment read model пользователя (`tenantId + externalUserId`).
- Добавлен `OTP/Delivery/Admin Backend Scope for Admin UI MVP.md`: backend scope разложен на `admin auth`, current enrollment read endpoint, admin-facing command endpoints, operator audit contour и hardening/test slices.
- Реализована Iteration 1 для `Admin UI MVP` backend: добавлены `auth.admin_users`, `auth.admin_user_permissions`, `AdminLoginHandler`, `PostgresAdminUserStore`, cookie-based `admin auth contour` и отдельный `AdminContext`.
- Добавлены `GET /api/v1/admin/auth/csrf-token`, `POST /api/v1/admin/auth/login`, `POST /api/v1/admin/auth/logout`, `GET /api/v1/admin/auth/session`, а state-changing admin endpoints теперь защищены `CSRF`.
- Добавлены login brute-force guard, sanitized audit события `admin_auth.login_succeeded|failed|rate_limited|logout_succeeded` и новые unit/endpoint tests; последовательная backend-проверка проходит с `147/147`.
- Реализована Iteration 2 для `Admin UI MVP` backend: добавлены `GetCurrentTotpEnrollmentForAdminHandler` и `GET /api/v1/admin/tenants/{tenantId}/users/{externalUserId}/enrollments/totp/current` как отдельный operator lookup/read path.
- Для admin read model добавлен `auth.totp_enrollments.revoked_utc`, чтобы revoked enrollment summary возвращал фактическое время revoke без вывода его из `updated_utc`.
- Добавлены unit/endpoint tests для admin current enrollment lookup (`401/403/404`, secret non-leakage, revoke metadata); последовательная backend-проверка проходит с `154/154`.
- Принят `ADR-028`: admin `start` использует явный `applicationClientId` или auto-resolve только при единственном активном integration client-е tenant-а; неоднозначность переводится в fail-closed `409`.
- Реализована Iteration 3 для `Admin UI MVP` backend: добавлены `POST /api/v1/admin/enrollments/totp`, `POST /api/v1/admin/enrollments/totp/{enrollmentId}/confirm`, `POST /api/v1/admin/enrollments/totp/{enrollmentId}/replace`, `POST /api/v1/admin/enrollments/totp/{enrollmentId}/revoke`.
- Для admin command transport добавлен отдельный by-id admin lookup в provisioning store, чтобы `confirm/replace/revoke` не зависели от `IntegrationClientContext`.
- Operator actions теперь пишут sanitized append-only audit события `admin_totp_enrollment.*` с `adminUserId`; endpoint tests закрывают `401/403/404/409`, `CSRF` и command-path audit writes, а backend verification проходит с `167/167`.
- В `admin` собран runtime `Admin UI MVP` shell: новый `app` entry point, `features/auth`, enrollment workspace, status/action panels и typed API client для `/api/v1/admin/*`.
- Frontend security contour закреплен через `credentials: include`, отдельный `CSRF` bootstrap, отсутствие `localStorage` для provisioning artifacts и discard semantics после перехода в read flow.
- Добавлены unit tests `problem-messages.test.ts`, `provisioning-artifact.test.ts` и `admin-api.test.ts`; `admin` проходит `npm test` (`7/7`) и `npm run build`.
- Frontend API client дополнительно ужесточен: admin command paths теперь `encodeURIComponent` для `enrollmentId`, а transport failures маппятся в явное operator-facing сообщение вместо неявного падения на cast к `AdminApiError`.
- Для `admin` добавлены `jsdom`/Testing Library component tests: `LoginPanel.test.tsx`, `EnrollmentStatusCard.test.tsx` и `EnrollmentPanels.test.tsx` на `start/confirm/replace/revoke`.
- Frontend test contour после этого шага проходит с `15/15` и теперь закрывает helper-, API- и component-level сценарии для runtime `Admin UI MVP`.
- Для локальной UI-проверки добавлен `Vite` proxy `/api -> http://127.0.0.1:5112`; dev server на `http://127.0.0.1:4173` отвечает `200`.
- Попытка обязательной browser-проверки через `Playwright MCP` повторно уперлась во внешний сбой `Transport closed`; это зафиксировано как текущий блокер regression suite, а не дефект самого `Admin UI`.
- Повторная попытка после добавления component tests дала тот же результат: `Playwright MCP` падает на первом вызове при живом `Vite` dev server, что подтверждает внешний инструментальный блокер.
- Пользовательский скриншот `img/1.png` показал непрерывные `500` на `GET /api/v1/admin/auth/session`; причина оказалась во frontend bootstrap-loop внутри `useAdminSession`, а не в самом layout-е страницы.
- `useAdminSession` исправлен: initial session restore теперь выполняется одним effect-run и не перезапускается после собственного перехода в `anonymous/error`.
- Добавлен regression test `useAdminSession.test.tsx`; frontend test contour после фикса проходит с `16/16`, `npm run build` остается зеленым.
- Найден реальный источник `Transport closed`: предыдущий `browser_run_code` уронил `Playwright MCP` через `ReferenceError: URL is not defined`; после restart `Codex` transport восстановился.
- Для live browser verification выявлен runtime blocker не во frontend, а в отсутствии backend на `127.0.0.1:5112`; через `codex-tui.log` восстановлена рабочая строка подключения к `dt-auth`.
- В `OtpAuth.Migrations` добавлены operational команды `list-admin-users` и `upsert-admin-user`; bootstrap admin password читается только из `OTPAUTH_ADMIN_PASSWORD`, permissions fail-closed ограничены whitelist-ом `enrollments.read|write`.
- Актуальные миграции применены к `dt-auth`, создан bootstrap admin user `operator`, а локальный `OtpAuth.Api` поднят на `http://127.0.0.1:5112` с dev `TOTP` key для нового external user.
- Обязательная browser verification через `Playwright MCP` теперь реально пройдена: `logout/login`, `start -> confirm`, `replace -> confirm`, `revoke` и `Load current` успешно отработали против живых `/api/v1/admin/*`; в console остался только косметический `404` на `favicon.ico`.
- В `admin` добавлен checked-in `Playwright` contour: `@playwright/test`, `playwright.config.ts`, `npm run test:e2e`, scripted fixture для `/api/v1/admin/*` и e2e flow `login -> start -> confirm -> reload/load current -> replace -> confirm -> revoke -> logout`.
- Для scripted contour вынесен test-only `TOTP` helper в `admin/test-support/totp-code.ts`, а `Vitest` дополнен unit tests на RFC-вектор и tolerant validation соседних time-step-ов.
- Локальная проверка нового frontend contour проходит полностью: `npm test` (`19` tests), `npm run build` и `npm run test:e2e` зеленые без зависимости от live bootstrap credentials.
- Добавлен `OTP/Delivery/Installer MVP Plan.md`: зафиксирован минимальный design contract для первого `Docker Compose`-based installer с preflight checks, install-time secret boundary, reuse `OtpAuth.Migrations`, idempotent bootstrap sequence и handoff в runtime `Admin UI`.
- В `infra/` реализован первый packaging slice для `Installer MVP`: добавлены `Dockerfile` для `api/worker/admin/bootstrap`, `docker-compose.yml`, `HTTPS` nginx edge для `Admin UI`, example runtime env contract и automated packaging contract checks; локально подтверждены `docker compose config`, `backend` sequential verify, `admin` tests/build.
- Поверх packaging slice добавлен первый installer entry point `infra/scripts/install.ps1`: preflight проверяет `docker`/`docker compose`, writable env directory, TLS files и operator HTTPS port; bootstrap и runtime orchestration идут через `docker compose` и `OtpAuth.Migrations`, а helper-логика покрыта unit-style tests в `infra/tests/installer.common.tests.ps1`.
- Installer расширен до operation modes `Install|Update|Recover`: `Update` повторно строит образы и прогоняет миграции без bootstrap admin, `Recover` поднимает существующий runtime contour без rebuild/bootstrap, а installer после запуска печатает sanitized `docker compose ps` вместо раскрытия полного compose render.
- Добавлен vault-first runbook `OTP/Delivery/Installer Operations Runbook.md` с безопасным operational порядком для `install/update/recover`; следующая зона работы сужена до richer diagnostics и recovery troubleshooting для `worker`.
- Для `OtpAuth.Worker` добавлен heartbeat diagnostics slice: worker пишет sanitized heartbeat file, compose читает его через healthcheck, а отдельный `OtpAuth.Worker.Tests` проект покрывает write/replace и invalid interval scenarios.
- Worker diagnostics доведен до domain-aware execution snapshot: snapshot теперь содержит `executionOutcome`, `consecutiveFailureCount` и sanitized dependency probe results для `Postgres/Redis`; `OtpAuth.Worker.Tests` расширен coordinator/parser тестами, а runbook теперь объясняет, как отличать dependency incident от просто живого контейнера.
- Worker diagnostics доведен до job-level snapshot: добавлен generic `IWorkerJob` contract, heartbeat теперь публикует `jobStatuses`, а первым реальным background job стал `security_data_cleanup` с sanitized cleanup metrics без утечки identifiers или secret material.
- Принят `ADR-029`: installer не уходит ни в `только UI`, ни в `только scripts`; source of truth закреплен за `infra/scripts/install.ps1` и `Installer.Common.ps1`, а отдельный installer UI допускается только как локальный setup wizard вне runtime `admin/`.
- В `OTP/Delivery/Installer MVP Plan.md` добавлен итерационный план по installer-треку: сначала hardening engine-контракта, затем recovery/diagnostics, затем отдельный local UI shell и только потом UI completion; после каждой итерации зафиксирован явный checkpoint, где лучше завершать сессию и чистить контекст.
- Завершена `Iteration 1` installer-трека: выделен `infra/scripts/Installer.Contract.ps1`, `install.ps1` теперь строит machine-readable manifest и умеет писать sanitized JSON-report через `-ReportJsonPath`, а validation и execution results стали структурированными.
- `infra/tests/installer.common.tests.ps1` усилен проверкой stable step ids, structured validation issues и отсутствия secret material в JSON-report; `infra/tests/packaging.contract.tests.ps1` обновлен под новый installer contract, оба тестовых контура проходят зелеными.
- Завершена `Iteration 2` installer-трека: добавлен `infra/scripts/Installer.Diagnostics.ps1`, installer теперь собирает structured runtime status через `docker compose ps --format json`, подтягивает sanitized `worker` heartbeat snapshot и пишет `TroubleshootingHints`/`DiagnosticIssues` в JSON-report без раскрытия secret material.
- `install/update/recover` path усилен через `docker compose up -d --wait worker`, чтобы unhealthy background runtime не проходил как ложный успех; `installer.common.tests.ps1` дополнен проверками worker wait-step, structured diagnostics и degraded report outcome.
- Выбран целевой UI-вариант для `Iteration 3`: отдельный loopback-only web shell поверх уже готового engine/report contract, а не `Electron/Tauri` и не расширение runtime `admin/`.
- Добавлен новый root `installer-ui`: `React + Vite` shell, локальный Node bridge на `127.0.0.1`, mock/live modes, submit в `infra/scripts/install.ps1` и очистка bootstrap password из UI state после запуска.
- Bridge передает bootstrap password только через process-level `OTPAUTH_ADMIN_PASSWORD`, не пишет секреты в report и не дублирует orchestration rules из `install.ps1`.
- Для `installer-ui` добавлены unit tests (`installer-api`, `InstallerRunForm`, `InstallerReportView`) и checked-in `Playwright` scenario `installer-shell.spec.ts`; локально подтверждены `npm test`, `npm run build` и `npm run test:e2e`.
- Continuation checkpoint для следующей сессии:
  - Installer track сейчас находится на стадии `packaging + installer entry point + update/recover modes + structured runtime diagnostics + local installer UI shell`.
  - Готовые артефакты: `infra/docker-compose.yml`, `infra/docker/*.Dockerfile`, `infra/nginx/admin.conf`, `infra/env/runtime.env.example`, `infra/scripts/install.ps1`, `infra/scripts/Installer.Contract.ps1`, `infra/scripts/Installer.Common.ps1`, `infra/scripts/Installer.Diagnostics.ps1`, `infra/tests/packaging.contract.tests.ps1`, `infra/tests/installer.common.tests.ps1`, `installer-ui/*`, `OTP/Delivery/Installer Operations Runbook.md`, `backend/OtpAuth.Worker.Tests`.
  - `installer-ui` уже умеет: локальный loopback-only shell, mode/form input для `Install|Update|Recover`, mock/live bridge, submit в `install.ps1`, password handoff только через process env, рендер validation issues, step results, runtime services, worker diagnostics и troubleshooting hints из sanitized report.
  - Важный security detail: preflight валидирует compose через `docker compose config --quiet`; полный render config больше не печатается, чтобы не утекали секреты.
  - Последняя проверка, которую не нужно повторно переоткрывать в vault: `packaging.contract.tests.ps1` зеленый, `installer.common.tests.ps1` зеленый, `installer-ui` проходит `npm test`, `npm run build`, `npm run test:e2e`.
  - Следующий практический подэтап: переходить к `Iteration 4` и закрывать полный happy path `install/update/recover` через UI, guardrails и documentation closure, не переписывая заново bridge/engine контракт.
  - Не ломать текущую security boundary: секреты остаются вне repo, `OTPAUTH_ADMIN_PASSWORD` только через process env, installer не должен печатать `ConnectionStrings__Postgres`, signing keys, `TotpProtection` keys или полный compose render.
- Завершена `Iteration 4` installer-трека: `installer-ui` теперь типизирует `OperationProfile/Manifest/Configuration`, добавляет mode-aware guardrails и доводит report до operational closure с handoff в runtime `Admin UI`.
- `installer-ui` после Iteration 4 проходит `npm test` (`7` tests), `npm run build` и `npm run test:e2e`; внутри sandbox `Playwright` упирался в локальный `EPERM`, поэтому browser regression был подтвержден разовым запуском вне sandbox.
- Локальный preview bridge, поднятый для ручной Playwright-проверки, был остановлен сразу после диагностики зависания; фоновый процесс не оставлен в рабочем состоянии.
- Installer track теперь можно считать закрытым на уровне `Installer MVP`; оставшийся setup-plane gap в delivery roadmap — отдельный `Bootstrap Agent`, а не UI completion вокруг текущего shell.
- Зафиксирован канонический mobile checkpoint `OTP/Product/Android TOTP-First Plan.md` для следующего трека после промежуточной очистки контекста.
- В `Android App Bootstrap` обновлен фактический workspace path после переноса monorepo: `D:\Projects\2026\DT-1520-Authenticator\mobile`.
- Для `mobile` принят первый clean module layout: `:app`, `:core:ui`, `:feature:provisioning`, `:feature:totp-codes`, `:security:storage`, `:totp-domain`.
- Следующий практический шаг после очистки контекста: не возвращаться к installer/admin, а начинать `Step 3` из `Android TOTP-First Plan` — secure storage abstraction поверх `Android Keystore`.
- `Step 3` mobile-трека завершен: в `mobile/security/storage` добавлены публичный `SecureTotpSecretStore`, Android `Keystore`-backed реализация через `SharedPreferences + AES/GCM`, hashed storage keys, fail-closed corrupted-record handling и unit tests на validation/serialization/restore/delete.
- В корневом `AGENTS.md` зафиксировано правило: для задач по `Android`, проблем `Gradle/Android Studio`, IDE sync и runtime-debugging нужно использовать `Android Studio MCP`, если он доступен.
- Для `mobile` через `Android Studio MCP` подтверждена доступность Android run configuration `app`, после чего `Step 4` реализован уже в чистом `totp-domain`, а не через Android-specific слой.
- `Step 4` mobile-трека завершен: в `mobile/totp-domain` добавлены `OtpAuthUriParser`, `TotpSecret`, `TotpCredential`, behavior-based `TotpAlgorithm`, `TotpCodeGenerator` и `TotpCodeState`.
- Security review для `Step 4` пройден: secret-bearing типы не используют `data class`, `toString()` redacted, duplicate query params и invalid Base32/percent-encoding валятся fail-closed, а тесты проверяют отсутствие утечки секрета в string representation.
- Локальная проверка mobile contour после `Step 4` проходит: `:totp-domain:test` зеленый; затем полный mobile contour `:app:testDebugUnitTest :core:ui:assembleDebug :feature:provisioning:testDebugUnitTest :feature:totp-codes:testDebugUnitTest :security:storage:testDebugUnitTest :totp-domain:test` тоже зеленый.
- Следующий практический шаг после `Step 4` был смещен на `Step 5` из `Android TOTP-First Plan`: provisioning/import flow поверх уже готового parser/domain engine.
- Для `Step 5` сначала найден и исправлен функциональный/security gap: `security:storage` сохранял только `account + secret`, что теряло `digits + algorithm` из provisioning contract; snapshot и tests обновлены под полный `TOTP` metadata set.
- `Step 5` mobile-трека завершен: в `feature:provisioning` добавлены masked inputs для `otpauth://` и manual fallback, preview/reducer layer, confirm/save flow и app wiring в `AndroidKeystoreSecureTotpSecretStore`.
- Security review для `Step 5` пройден: provisioning inputs не сохраняются через `rememberSaveable`, URI/manual secret masked в UI, preview не печатает secret material, storage callback boundary не раскрывает `Keystore` implementation в feature, а save errors остаются generic и не leak-ают внутренние детали.
- Локальная проверка после `Step 5` проходит: `:totp-domain:test :security:storage:testDebugUnitTest :feature:provisioning:testDebugUnitTest :feature:totp-codes:testDebugUnitTest :app:testDebugUnitTest` зеленый; затем полный mobile contour с `:core:ui:assembleDebug` тоже зеленый.
- Следующий практический шаг теперь смещен на `Step 6` из `Android TOTP-First Plan`: runtime codes screen со списком сохраненных аккаунтов, текущим кодом, countdown и local remove flow.
- `Step 6` mobile-трека завершен: `feature:totp-codes` получил runtime presenter, empty/runtime UI state и двухшаговый remove confirm flow; `AuthenticatorApp` теперь читает secure store snapshot через fail-closed helper и не держит последний provisioning preview после save.
- Во время live Android-проверки найден реальный UX-баг: нижняя часть runtime-card была недостижима на устройстве, потому что `app` shell не скроллился. Исправлено через `verticalScroll` на корневом `Column`, после чего `remove` flow стал доступен на `emulator-5554`.
- Live verification через `Android Studio MCP` + `the_android_mcp` + `mobile_mcp` подтверждена: manual fallback import -> preview -> save -> runtime code/countdown -> remove confirm state на эмуляторе `emulator-5554`.
- Следующий практический шаг теперь смещен на `Step 7` из `Android TOTP-First Plan`: backend/vault contract sync для stable provisioning contract и references на `secretUri`.
- Continuation checkpoint после очистки контекста:
- читать в порядке `OTP/01 - Current State.md -> OTP/Agent/Implementation Map.md -> OTP/Product/Android TOTP-First Plan.md`
- продолжать с `Step 6`, не возвращаясь к installer/admin/backend трекам
- в коде сначала смотреть `mobile/app/.../AuthenticatorApp.kt`, `mobile/feature/totp-codes/*`, `mobile/feature/provisioning/*`, `mobile/security/storage/*`, `mobile/totp-domain/*`
- цель следующего шага: заменить placeholder runtime code screen на список сохраненных аккаунтов, реальный текущий код, countdown и local remove flow без повторного раскрытия provisioning artifact
- `Step 7` mobile-трека завершен: добавлена каноническая заметка `OTP/Integrations/TOTP Provisioning Contract.md`, которая фиксирует visibility rules и текущую семантику `secretUri/qrCodePayload` для backend, `Admin UI` и `Android TOTP-first`.
- `OpenAPI` уточнен без расширения scope: schema `TotpEnrollment` теперь явно описывает `secretUri` и `qrCodePayload` как artifact-only поля, ожидаемые только в `start/replace`.
- `Product`, `Delivery` и `Security` заметки синхронизированы с этим contract note; `Current State` и `Implementation Map` обновлены под фактическое завершение `Step 7`.
- Security review для документационного sync не выявил новых рисков: контракт по-прежнему требует one-time display semantics и запрещает повторное раскрытие provisioning artifact через read models, audit payloads и persistent client cache.
- Следующий практический шаг теперь смещен с contract sync на `План добивания mobile после Step 7`: базовые `Compose` UI/instrumented tests для provisioning/runtime screens, полный mobile regression contour и финальное `Android TOTP-first` DoD closure.
- Continuation checkpoint после следующей очистки контекста:
- читать в порядке `OTP/01 - Current State.md -> OTP/Agent/Implementation Map.md -> OTP/Product/Android TOTP-First Plan.md -> OTP/Integrations/TOTP Provisioning Contract.md`
- продолжать с `План добивания mobile после Step 7`, не возвращаясь к уже закрытым `Step 1-7`
- в коде сначала смотреть `mobile/app/.../AuthenticatorApp.kt`, `mobile/feature/provisioning/*`, `mobile/feature/totp-codes/*` и существующий `mobile/app/src/androidTest/*` contour
- В `P0` mobile-трека выполнен vault/code gap check: подтверждено, что `ProvisioningRouteUiTest` и `TotpCodesRouteUiTest` уже существуют, поэтому следующий security hardening делался поверх реального test contour, а не по предположению о его отсутствии.
- Закрыт `Android` backup/security gap для локального `TOTP-first`: `AndroidManifest.xml` переведен на `android:allowBackup="false"`, а `backup_rules.xml` и `data_extraction_rules.xml` теперь явно запрещают cloud backup и `device-transfer` для всех relevant domains как defense-in-depth против Android 12+ OEM variance.
- В `feature:provisioning` добавлен whitelist для validation copy: неожиданный текст исключения больше не выходит в UI error path, что убирает риск случайно показать сырой provisioning input с secret material.
- После `P0` hardening локально подтверждены `:app:testDebugUnitTest`, `:feature:provisioning:testDebugUnitTest` и `:app:assembleDebug`; дополнительно зафиксировано рабочее правило: если `the_android_mcp` возвращает `NO_DEVICES_FOUND`, нужно сначала запускать эмулятор и только потом выполнять `MCP`/live verification, а не считать проверку пропущенной по внешней причине.
- Для `mobile` добавлен app-level instrumented contour `mobile/app/src/androidTest/java/ru/dt1520/security/authenticator/app/AuthenticatorAppUiTest.kt`, который проходит сценарий `manual import -> preview -> save -> remove -> empty state` через in-memory `SecureTotpSecretStore`.
- `ProvisioningRouteUiTest` усилен проверкой generic save error без утечки raw exception текста, а `TotpCodesRouteUiTest` дополнен runtime-проверкой sorted summaries/code/countdown и стабилизирован по remove callback через `waitUntil` вместо зависания на `waitForIdle`.
- На `emulator-5554` точечно подтверждены `ProvisioningRouteUiTest`, `TotpCodesRouteUiTest` и `AuthenticatorAppUiTest`; long-running общий `connectedDebugAndroidTest` не использовался, чтобы не ловить ложные зависания без промежуточной телеметрии.
- Повторная live MCP-проверка после зеленых `androidTest` сорвалась на окружении: `the_android_mcp`/`uiautomator` падают с `UiAutomationService ... already registered`, затем эмулятор показывает `Process system isn't responding` и черный экран. Логи указывают на environment-level `UiAutomation` conflict и нестабильность эмулятора, а не на новую продуктовую ошибку в `mobile`.
- После явного cleanup `emulator/qemu/adb/java` подтверждено отсутствие хвостов: `Get-Process` пустой, `the_android_mcp.list_android_devices` возвращает `NO_DEVICES_FOUND`; затем поднят новый `Pixel_10_Pro` и использован fresh `emulator-5554`.
- На clean emulator state live MCP flow успешно пройден визуально без `uiautomator dump`: `manual issuer/account/secret -> Preview manual import -> Save secure secret -> runtime code/countdown -> Удалить с устройства -> Подтвердить удаление -> empty state`.
- Вывод по environment: предыдущий `UiAutomationService ... already registered` был следствием грязного состояния эмулятора, а не дефектом `mobile`; для следующих Android live-rerun имеет смысл сначала срезать все хвосты процессов, а уже потом повторять MCP-проверку.
- `Android TOTP-first` теперь считается закрытым локальным slice: `P0/P1/P2` доведены до конца, vault синхронизирован с фактическим mobile state, а следующий рабочий фокус смещен на `backup codes` backend slice.
- Реализован первый `backup codes` backend slice: добавлены `auth.backup_codes`, `POST /api/v1/challenges/{id}/verify-backup-code`, hash-only PBKDF2 storage, atomic single-use consume path, explicit `seed-bootstrap-backup-codes` и unit coverage; следующий фокус теперь смещен на device lifecycle design/contracts.
- Зафиксирован `device lifecycle design/contracts` slice: принят `ADR-030`, добавлена каноническая заметка `OTP/Architecture/Device Lifecycle Design.md`, а `Security/Data/Auth flows/OpenAPI` синхронизированы под модель `pending/active/revoked/blocked`, `last_auth_state_changed_utc` и opaque rotating hash-only refresh tokens; следующий фактический backend шаг теперь `Device Registry` runtime slice `activate -> refresh -> revoke`.
- Реализован runtime `Device Registry` slice: добавлены `POST /api/v1/devices/activate`, `POST /api/v1/auth/device-tokens/refresh`, `POST /api/v1/devices/{deviceId}/revoke`, миграция `auth.devices + auth.device_refresh_tokens + auth.device_activation_codes`, separate device JWT auth contour и bootstrap command `seed-bootstrap-device-activation`.
- Security review для device slice закрыт по критическим инвариантам: activation artifact хранится hash-only и consume-ится atomically, refresh token rotate-ится на каждый successful refresh, replay/expired/revoked refresh token fail-closed переводит устройство в `blocked`, audit payload не содержит refresh/access token, activation code, `pushToken` и целый `publicKey`.
- Локальная проверка после device implementation проходит: `dotnet test OtpAuth.Infrastructure.Tests` зеленый (`197/197`), `scripts/verify-backend.ps1` зеленый, новый contour покрыт unit + endpoint tests для `activate/revoke/refresh/replay/runtime validation`.
- Принят `ADR-031`: `push` challenge теперь считается корректным только при явном server-side binding к одному active device; approve/deny другим device bearer fail-closed запрещены.
- Реализован device-bound `push approve/deny` contour: backend публикует `POST /api/v1/challenges/{id}/approve` и `POST /api/v1/challenges/{id}/deny` под `DeviceBearer`, challenge persistence хранит `target_device_id + approved_utc + denied_utc`, approve требует `biometricVerified=true`, а create-path auto-bind-ит `push` только при единственном active push-capable device пользователя.
- Security review для push slice закрыт по критическим инвариантам: approve/deny не принимаются без binding к `target_device_id`, raw `reason` не попадает в audit payload, `DeviceBearer` не переиспользует integration auth contour, multi-device ambiguity fail-closed уводит create-path в `TOTP` fallback вместо неявного выбора device.
- Локальная проверка после push implementation проходит: `dotnet test OtpAuth.Infrastructure.Tests` зеленый (`211/211`), `scripts/verify-backend.ps1` зеленый, новый contour покрыт unit + endpoint tests для binding, `biometricVerified`, policy fallback, expired/not-found и deny paths.
- Реализован `push delivery` slice: `CreateChallenge` теперь принимает optional `targetDeviceId`, атомарно пишет `push` challenge вместе с row в `auth.push_challenge_deliveries`, а `OtpAuth.Worker` новым job `push_challenge_delivery` lease-ит due deliveries, dispatch-ит sanitized request в gateway и пишет `delivered/rescheduled/failed` status.
- Реализован explicit support path для multi-device routing: `GET /api/v1/devices?externalUserId=...&pushCapableOnly=true` возвращает active devices с `isPushCapable`, после чего integration может детерминированно создать `push` challenge с нужным `targetDeviceId` вместо fail-open/implicit выбора устройства.
- Security review для delivery/routing slice закрыт по критическим инвариантам: raw `pushToken` не дублируется в outbox table, multi-device ambiguity без explicit `targetDeviceId` по-прежнему fail-closed уводит create-path в `TOTP`, worker перед dispatch повторно валидирует `challenge + device binding`, а permanent delivery failure не делает silent reroute на другой device.
- Локальная проверка после `push delivery + routing` проходит: `scripts/verify-backend.ps1` зеленый, `OtpAuth.Infrastructure.Tests` зеленый (`217/217`), `OtpAuth.Worker.Tests` зеленый (`15/15`).
- Зафиксирован `OTP/Product/Android Push Runtime Plan.md`; трек разложен на `Iteration 1-4`, а первый срез осознанно ограничен backend/device contract для pending `push` inbox, чтобы mobile runtime не строился поверх временного API.
- Завершена `Iteration 1` `Android Push Runtime`: backend публикует `GET /api/v1/devices/me/challenges/pending` под `DeviceBearer`, read path отдает только active pending `push` challenges для authenticated device, а `OpenAPI`, `Auth and Token Flows`, `Device Lifecycle Design`, `Current State` и `Implementation Map` синхронизированы.
- Automated coverage для первого push-runtime slice усилена unit + endpoint tests: scope boundary, ordering и filter behavior подтверждены без раскрытия чужих/expired/non-pending challenge records.
- Завершена `Iteration 2` `Android Push Runtime`: в `mobile` добавлен новый модуль `:feature:push-approvals` с `PendingPushApproval`, pure presenter/workflow, empty/runtime Compose shell и app wiring через injected read-model/decision callbacks без transport coupling.
- Security review для mobile shell не выявил новых критичных рисков: feature не хранит device tokens, не знает про HTTP/refresh, показывает только sanitized pending challenge metadata и схлопывает transport failures в generic error copy без утечки backend/internal details.
- Локальная проверка после Iteration 2 проходит: `:feature:push-approvals:testDebugUnitTest`, `:app:testDebugUnitTest` и `:app:assembleDebug` зеленые; следующий практический шаг теперь `Iteration 3` с device session/transport layer.
- Завершена `Iteration 3` `Android Push Runtime`: в `mobile/security:storage` добавлены encrypted `installationId + device session`, а в `mobile/app` собраны `DeviceRuntimeSessionManager` и `HttpDeviceRuntimeTransport` для `activate/refresh/pending/approve/deny` с proactive/401-driven refresh и fail-closed reset при invalidated session.
- Security review для `Iteration 3` закрыт по критичным инвариантам: device access/refresh token не пишутся в plaintext storage, installation identity хранится отдельно от session clear semantics, refresh conflict/unauthorized path стирает persisted session, а UI получает только generic status copy без backend/internal detail leakage.
- Локальная проверка после `Iteration 3` проходит: `:security:storage:testDebugUnitTest`, `:feature:push-approvals:testDebugUnitTest`, `:app:testDebugUnitTest`, `:app:assembleDebug` зеленые.
- Live Android verification через `the_android_mcp` подтверждена на `emulator-5554`: свежий `app-debug.apk` установлен, `ru.dt1520.security.authenticator/.MainActivity` стартует, UI dump показывает pending push section с новым runtime copy, приложение остается в foreground, а `logcat` не содержит error-level записей для пакета.
- В `AGENTS.md` добавлено явное project-level правило: для задач по `Android` пользователь заранее разрешает запуск `Gradle`/`adb`/emulator-скриптов, установку необходимого Android tooling/SDK-зависимостей, сборки, тесты, поднятие эмулятора и live verification; отдельно оговорено, что внешние sandbox/runtime ограничения все равно нужно явно обозначать как ограничения среды, а не проекта.
- Завершена `Iteration 4` `Android Push Runtime`: approve теперь проходит через AndroidX `BiometricPrompt`, а recent decisions пишутся в encrypted sanitized local history без transport-данных; `PushApprovalsRoute` рендерит history section и typed safe failures.
- Security review для `Iteration 4` закрыт по основным инвариантам: biometric gate fail-closed не пропускает approve при cancel/unavailable, local history не хранит access/refresh token, `pushToken`, correlation или backend/internal error detail, а corruption encrypted history не ломает сам approve path.
- Локальная проверка после `Iteration 4` проходит: `:security:storage:testDebugUnitTest`, `:feature:push-approvals:testDebugUnitTest`, `:app:testDebugUnitTest` зеленые; `:app:connectedDebugAndroidTest` зеленый на `emulator-5554`.
- Live MCP verification после установки свежего `app-debug.apk` подтверждает новый push runtime shell на `emulator-5554`: `Ожидающие push-запросы`, empty-state для pending inbox и empty-state локальной истории реально рендерятся в обычном `MainActivity` вне test runner.
- Выполнен vault consistency cleanup после закрытия `Android Push Runtime Iteration 4`: выровнены `Start Here`, `Current State`, `Implementation Map`, `Mobile App`, `Device Lifecycle Design`, `Implementation Plan 8-12 Weeks` и `Installer MVP Plan`, чтобы канонические Obsidian-заметки больше не описывали проект как scaffold-only или как будто `Iteration 4` еще открыта.
- В `P1` security-pass для `Android Push Runtime` убран лишний device-facing metadata leak: pending inbox contract и mobile UI больше не получают `correlationId`, потому что для device runtime это internal tracing identifier, а не operator-facing payload.
- После рефакторинга `AuthenticatorApp` `P0` подтвержден повторно: `verify-backend.ps1` зеленый (`222` infra + `15` worker), `:feature:push-approvals:testDebugUnitTest` и `:app:testDebugUnitTest` зеленые, `:app:connectedDebugAndroidTest` проходит `12/12` на `emulator-5554`.
- Повторный mobile compile failure с `unresolved reference` в `AuthenticatorModuleWiringTest` оказался environment artifact грязного `Gradle` state: после остановки зависших `java`-процессов и удаления `gradle-9.3.1-bin.zip.lck` targeted unit contour снова стал зеленым без дополнительных кодовых правок.
- `P1` security-pass для `Android Push Runtime` теперь закрыт end-to-end: device-facing pending contract sanitized, backend по-прежнему фильтрует только `tenant + client + target_device + pending + push + not expired`, а mobile UI не пробрасывает backend/internal error details пользователю.
- Continuation checkpoint после следующей очистки контекста:
- читать в порядке `OTP/01 - Current State.md -> OTP/Agent/Implementation Map.md -> OTP/Product/Android Push Runtime Plan.md -> OTP/Architecture/Device Lifecycle Design.md`
- считать `P0` и `P1` закрытыми, не возвращаться к refactor/security-pass без нового факта
- следующий рабочий шаг: `P2` как чистая фиксация рабочего дерева, review diff и разделение commit-ов на `backend/mobile` и `OTP/AGENTS`
- `P2` закрыт: review рабочего дерева подтвердил, что в diff нет посторонних файлов или generated artifacts, а фиксация готовится двумя логическими commit-ами: сначала `backend/mobile` код + тесты, затем синхронизация `OTP` по `P0/P1`.
- `P2` завершен полностью: рабочее дерево чистое, `backend/mobile` зафиксированы коммитом `b7e1eba` (`Refactor and harden Android push approvals runtime`), а vault sync зафиксирован коммитом `1c13c6c` (`Sync vault for Android push runtime hardening`).
- Continuation checkpoint после очистки контекста:
- считать `Android Push Runtime` локально закрытым до нового факта; не возвращаться к `P0/P1/P2`
- начинать следующий рабочий цикл не с cleanup, а с нового roadmap-трека
- ближайший логичный приоритет: `push delivery observability/hardening` в backend contour (`webhook/events`, observability/alerts, pilot integration scenario, replay/delivery hardening)
- Закрыт первый внешний callback/webhook slice поверх challenge lifecycle: terminal state transitions `approved/denied/expired` теперь атомарно enqueue-ят `auth.challenge_callback_deliveries`, а `OtpAuth.Worker` доставляет signed `HTTPS` callbacks с `X-OTPAuth-Signature`, bounded retry и fail-closed semantics.
- Усилен security boundary для `callbackUrl`: create-path теперь reject-ит `localhost` и private-network IP literals поверх обязательного `HTTPS`, чтобы убрать obvious SSRF path на этапе request validation.
- Локальная проверка после callback slice подтверждена: `OtpAuth.Infrastructure.Tests` проходят `231/231`, `OtpAuth.Worker.Tests` проходят `17/17`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Следующий логичный приоритет после этого среза сузился до двух дорожек: `provider-specific push gateway adapter` вместо logging gateway и отдельный top-level subscription/webhook model beyond текущих per-request challenge callbacks.
- Закрыт `provider-specific push gateway adapter`: добавлены configurable `PushDelivery:Provider`, `logging` fallback и `FCM HTTP v1` adapter поверх existing `IPushChallengeDeliveryGateway` без изменения `auth.push_challenge_deliveries` или worker/outbox contract.
- Security review для нового push adapter закрыт по критичным инвариантам: `FCM` принимает только `service_account` credentials, не логирует `pushToken/externalUserId/correlationId`, использует sanitized payload (`challengeId + operationType`) и делит transient/permanent provider failures на retryable/non-retryable без fail-open поведения.
- Локальная проверка после `FCM` adapter slice проходит: `OtpAuth.Infrastructure.Tests` зеленый (`240/240`), `OtpAuth.Worker.Tests` зеленый (`17/17`), `verify-backend.ps1` зеленый; residual warning остается прежним и связан только с `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Закрыт первый top-level `webhooks/events` backend slice: добавлены отдельные `auth.webhook_subscriptions`, `auth.webhook_subscription_event_types`, `auth.webhook_event_deliveries`, bootstrap commands `list-webhook-subscriptions` и `upsert-webhook-subscription`, worker job `webhook_event_delivery` и top-level `challenge.approved|denied|expired` fan-out beyond per-request callbacks.
- Security review для нового webhook contour закрыт по основным инвариантам: subscription endpoints требуют `HTTPS`, reject-ят `localhost` и private-network IP literals, outbound payload остается sanitized snapshot-ом, подпись идет через `HMAC-SHA256` в `X-OTPAuth-Signature`, а retry/fail path не зависит от повторной загрузки mutable domain state.
- Локальная проверка после top-level webhook slice подтверждена: `OtpAuth.Infrastructure.Tests` проходят `254/254`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Расширен тот же top-level `webhooks/events` contour на device lifecycle: `ActivateDeviceHandler`, `RevokeDeviceHandler` и fail-closed block path в `RefreshDeviceTokenHandler` теперь атомарно fan-out-ят `device.activated|revoked|blocked` через общий subscription/outbox storage без отдельного transport-а.
- Security review для device webhook expansion закрыт по критичным инвариантам: payload не содержит `pushToken`, `publicKey` или `installationId`, повторный `refresh` reuse публикует `device.blocked` только при реальном state transition, а `revoke` не создает лишний webhook для уже `revoked/blocked` device.
- Локальная проверка после device webhook expansion подтверждена: `OtpAuth.Infrastructure.Tests` проходят `261/261`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается прежним и связан только с `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Закрыт `factor.revoked` без введения нового transport-а: revoke `TOTP` enrollment-а теперь формирует `FactorRevocationSideEffects`, а provisioning store queue-ит top-level webhook delivery атомарно с persisted revoke update, поэтому integration и admin revoke-path больше не расходятся с `OpenAPI`/vault.
- Security review для `factor.revoked` закрыт по критичным инвариантам: payload остается sanitized (`factorType=totp`, `subject.externalUserId`), не содержит secret-bearing enrollment fields, а outbox publish не происходит при conflict path без реального state transition.
- Локальная проверка после `factor.revoked` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `264/264`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Следующий шаг по `ADR-033` закрыт: для top-level webhook contour добавлены admin endpoints `GET /api/v1/admin/tenants/{tenantId}/webhook-subscriptions` и `POST /api/v1/admin/webhook-subscriptions`, а permissions bootstrap whitelist расширен до `webhooks.read|write`.
- `Admin UI` теперь содержит отдельный workspace для webhook subscriptions: tenant-scoped `load`, create/update, редактирование набора `eventTypes`, deactivate через `isActive=false`, problem mapping и session-aware рендеринг рядом с enrollment workspace.
- Security review для нового admin webhook slice закрыт по основным инвариантам: reuse existing fail-closed `HTTPS`/private-network validation, явный tenant/application client resolution, state-changing calls под cookie session требуют `CSRF`, а admin audit пишет только sanitized metadata `admin_webhook_subscription.upserted|deactivated`.
- Локальная проверка после webhook management slice подтверждена: `backend/scripts/verify-backend.ps1` зеленый (`272` infra tests + `19` worker tests), `admin` проходит `npm test` (`22` tests), `npm run build` и `npm run test:e2e`; browser regression для enrollment flow и webhook flow подтверждена, а единственный residual warning остается прежним и связан только с `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Зафиксирован отдельный канонический continuation document `OTP/Delivery/MVP Closure Iteration Plan.md`, чтобы remaining путь до `MVP` больше не выбирался ad-hoc по ходу сессий.
- Remaining work декомпозирован на три последовательные итерации: `Iteration 1` delivery observability, `Iteration 2` device lifecycle admin/support, `Iteration 3` pilot integration + final hardening; новый functional scope вне этих треков до закрытия `MVP` брать не нужно.
- В план сразу встроены context-safe checkpoint-ы: одна сессия = один `slice`, после каждого `slice` обязательны tests/security review/vault write-back, а continuation reading path начинается с `Current State + Implementation Map + MVP Closure Iteration Plan`.
- Завершен `Iteration 1 / Slice 1A` из `MVP Closure Iteration Plan`: backend получил unified admin-facing read model для recent `challenge_callback` и `webhook_event` deliveries с фильтрами `tenant/application/channel/status` и без повторной загрузки mutable domain state.
- Security review для `Slice 1A` закрыт по критичным инвариантам: read model возвращает только sanitized destination без `userinfo/query/fragment`, не раскрывает secret material из callback/webhook URL и не требует reread mutable runtime сущностей для detail view.
- Локальная проверка после `Slice 1A` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `279/279`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Завершен `Iteration 1 / Slice 1B`: добавлен read-only admin endpoint `GET /api/v1/admin/tenants/{tenantId}/delivery-statuses` с optional filters `applicationClientId/channel/status/limit`, stable `400 Problem` для invalid filters и `404` для tenant-mismatched `applicationClientId`.
- Security review для `Slice 1B` закрыт по критичным инвариантам: permission surface не расширялся сверх `webhooks.read`, access denial остается fail-closed, а transport повторно sanitizes `deliveryDestination`, чтобы не пропустить `userinfo/query/fragment` наружу даже при ошибочном store payload.
- Локальная проверка после `Slice 1B` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `284/284`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Следующий continuation point после этого checkpoint-а: `Iteration 1 / Slice 1C` как operator workspace в `Admin UI` поверх уже готового delivery status API.
- Завершен `Iteration 1 / Slice 1C`: `Admin UI` получил новый workspace `delivery-statuses` поверх `GET /api/v1/admin/tenants/{tenantId}/delivery-statuses` с tenant/application filters, inventory list и read-only detail panel без replay/retry actions.
- Security review для `Slice 1C` закрыт по критичным инвариантам: reuse existing permission `webhooks.read`, client-side validation ограничена `tenantId/limit`, UI не раскрывает raw payload/response body и рендерит только already-sanitized destination/timing/error metadata из backend read model.
- Локальная UI-проверка после `Slice 1C` подтверждена: `admin` проходит `npm test` (`25` tests), `npm run build` и `npm run test:e2e` (`3` сценария, включая новый delivery-status browser regression).
- Следующий continuation point после этого checkpoint-а: `Iteration 1 / Slice 1D` как metrics baseline для `queued/delivered/failed/retrying`.
- Завершен `Iteration 1 / Slice 1D`: `challenge_callback` и `webhook_event` delivery stores теперь отдают aggregated summary по `queued/delivered/failed/retrying`, а worker jobs публикуют этот baseline в heartbeat/job metrics и отдельной sanitized summary log line по каждому channel.
- Security review для `Slice 1D` закрыт по критичным инвариантам: observability surface ограничен агрегированными count-ами и именем channel, без `destination URL`, payload, response body или сырого transport-error текста.
- Локальная backend-проверка после `Slice 1D` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `284/284`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; первый прогон внутри sandbox уперся в write-denied на `backend/artifacts/obj`, но rerun вне sandbox прошел без code changes, residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`.
- Следующий continuation point после этого checkpoint-а: `Iteration 2 / Slice 2A` как device admin read model поверх уже закрытого delivery observability contour.
- Завершен `Iteration 2 / Slice 2A`: backend получил `AdminUserDevice*` read model, `AdminListUserDevicesHandler`, `PostgresAdminDeviceStore`, `devices.read` permission boundary и индекс `ix_devices_tenant_external_user_status` для current/recent device lookup по `tenantId + externalUserId`.
- Security review для `Slice 2A` закрыт по критичным инвариантам: operator read model отдает только `deviceId/platform/status/timestamps/isPushCapable`, не раскрывает `pushToken`, `publicKey`, `installationId` или `deviceName`, а mapper fail-closed отклоняет неожиданный `pending` status как unsupported admin payload.
- Локальная backend-проверка после `Slice 2A` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `290/290`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; residual warning остается только старый `IBM.Data.Db2` architecture mismatch в `OtpAuth.Migrations`, а следующий continuation point смещен на `Iteration 2 / Slice 2B`.
- Завершен `Iteration 2 / Slice 2B`: backend получил admin device transport `GET /api/v1/admin/tenants/{tenantId}/users/{externalUserId}/devices` и `POST /api/v1/admin/tenants/{tenantId}/users/{externalUserId}/devices/{deviceId}/revoke`, плюс `AdminRevokeUserDeviceHandler` с `devices.write` boundary.
- Security review для `Slice 2B` закрыт по критичным инвариантам: revoke path требует `CSRF`, fail-closed валидирует `tenantId + externalUserId + deviceId`, не раскрывает `deviceName`, `pushToken`, `publicKey` или `installationId`, а non-active states ограничены безопасным `409` для уже `revoked|blocked`.
- Локальная backend-проверка после `Slice 2B` подтверждена: `OtpAuth.Infrastructure.Tests` проходят `302/302`, `OtpAuth.Worker.Tests` проходят `19/19`, `verify-backend.ps1` зеленый; внутри sandbox прогон упирался в `Access denied` на запись в `backend/artifacts/*`, поэтому verification была повторена вне sandbox без дополнительных code changes.
- Следующий continuation point после этого checkpoint-а: `Iteration 2 / Slice 2C` как admin UI workspace для user devices поверх уже закрытого admin device transport.
- Завершен `Iteration 2 / Slice 2C`: `Admin UI` получил новый workspace `user-devices` поверх admin device transport с lookup по `tenantId + externalUserId`, inventory list для `active|revoked|blocked` и detail/action panel с destructive confirmation перед revoke.
- Security review для `Slice 2C` закрыт по критичным инвариантам: UI использует только `devices.read|write`, revoke остается привязан к последнему успешно загруженному scope, не раскрывает `installationId`, `deviceName`, `pushToken` или `publicKey`, а non-active devices не получают новый support path сверх уже существующего `409`.
- Локальная UI-проверка после `Slice 2C` подтверждена: `admin` проходит `npm test` (`31/31`), `npm run build` и `npm run test:e2e` (`4/4`, включая новый device browser regression) без дополнительных sandbox escalation.
- Следующий continuation point после этого checkpoint-а: `Iteration 2 / Slice 2D` как audit/webhook consistency для admin device actions поверх уже закрытых `2A-2C`.
- Завершен `Iteration 2 / Slice 2D`: admin revoke path теперь пишет отдельный append-only `admin_device.revoked`, при этом existing `device.revoked` lifecycle audit и `device.revoked` webhook publication сохраняют прежние semantics.
- Security review для `Slice 2D` закрыт по критичным инвариантам: admin audit строится только из sanitized device metadata, не включает `installationId`, `deviceName`, `pushToken`, `publicKey` или transport/request details и не расширяет operator-visible revoke surface.
- Локальная backend-проверка после `Slice 2D` подтверждена: targeted admin/device tests проходят `13/13`, `verify-backend.ps1` зеленый, `OtpAuth.Infrastructure.Tests` проходят `303/303`, `OtpAuth.Worker.Tests` проходят `19/19`; sandbox снова блокировал `dotnet test` через `Access denied` на `backend/artifacts/*`, поэтому verification повторена вне sandbox.
- Следующий continuation point после этого checkpoint-а: `Iteration 3 / Slice 3A` как pilot scenario definition поверх уже закрытых `Iteration 1-2`.
- Завершен `Iteration 3 / Slice 3A` как design-only vault slice: canonical pilot app выбран как `ProjectManager`, primary auth остается в `Keycloak`, canonical `externalUserId` фиксируется как `Keycloak sub`, а первая protected operation ограничена create/update `VCS instance` credentials.
- Принят `ADR-034`: для pilot integrations не заменяем existing primary auth, а добавляем `Authenticator` как backend-driven step-up `MFA` contour только для sensitive operations; direct SPA-to-Authenticator secret-bearing path запрещен.
- Добавлена каноническая заметка `OTP/Delivery/ProjectManager Pilot Integration Story.md` и синхронизированы `Decision Index`, `Auth and Token Flows`, `Implementation Map`, `Current State` и `MVP Closure Iteration Plan`; следующий continuation point теперь смещен на `Iteration 3 / Slice 3B`.
- Повторная оценка внешнего сервера `ghostring` после уточнения вводных изменила вывод с условно-ограниченного на pilot-suitable: при нагрузке `<=10` пользователей, доступном `admin.ghostring.ru`, existing `dt-auth` и допустимой установке `docker compose` сервер признан пригодным для server-side rollout.
- Зафиксирован server-specific operational note `OTP/Delivery/Ghostring Pilot Deployment Profile.md`: accepted pilot profile использует existing host `nginx` как public edge, existing `PostgreSQL`/`dt-auth`, отдельный `Redis` и не использует compose-managed `postgres`.
- Под server-specific profile подготовлены concrete repo assets: `infra/docker-compose.ghostring.yml`, `infra/env/ghostring.runtime.env.example` и `infra/nginx/admin.ghostring.ru.conf.example`; `infra/README.md` дополнен отдельным `Ghostring Pilot Profile` path.
- Packaging contract checks расширены под `ghostring`-assets: тест теперь требует отдельный compose/env/nginx template, проверяет internal-only bind `127.0.0.1:18443`, отсутствие compose-managed `postgres` и наличие required signing keys для worker callback/webhook path.
- Подготовлен отдельный operational note `OTP/Delivery/Ghostring Pre-Deploy Checklist.md`: pre-deploy path теперь разложен на `Repository boundary -> compose -> PostgreSQL -> Redis -> keys -> internal TLS -> host nginx -> compose review -> bootstrap readiness -> smoke plan`.
- Следующий практический шаг после этой фиксации: заполнить runtime env, подготовить internal TLS material и пройти `Ghostring Pre-Deploy Checklist` перед первым реальным rollout.
- Выполнен controlled bring-up на `ghostring` без `nginx` cutover: подготовлен `/opt/dt-1520-authenticator/runtime.env`, сгенерирован internal TLS для `admin.ghostring.ru`, через `infra/docker-compose.ghostring.yml` успешно пройдены `ensure-database`, `migrate` и `upsert-admin-user` с pilot permissions `devices.read|write`, `enrollments.read|write`, `webhooks.read|write`.
- Runtime contour `redis + api + admin + worker` поднят локально и проходит smoke-check `https://127.0.0.1:18443/health/api -> 200`; для `admin` дополнительно исправлены host file permissions на bind-mounted TLS (`uid/gid 101`, directory traversal rights).
- Выявлены два operational residual-а перед публичным cutover: `api/worker` при старте логируют отсутствие `libgssapi_krb5.so.2`, но продолжают работать healthy, а `admin.ghostring.ru` пока не готов к host `nginx` cutover, потому что на `185.153.182.121:443` уже слушает `ocserv`, а не `nginx`.
- После реального ghostring cutover выявлен runtime blocker на `GET /api/v1/admin/auth/csrf-token`: `OtpAuth.Api` не видел `HTTPS` за цепочкой reverse proxy и antiforgery fail-closed ломал первый operator login.
- Блокер закрыт кодом, а не nginx workaround-ом: в `backend/OtpAuth.Api` добавлен trusted reverse-proxy helper `ReverseProxyForwardingOptions`, `Program.cs` теперь вызывает `UseForwardedHeaders()` только при явной конфигурации trusted proxies/networks, а secure cookies/CSRF policy не ослаблялись.
- Infra path синхронизирован под тот же fix: `infra/docker-compose.yml` и `infra/docker-compose.ghostring.yml` теперь задают deterministic `OTPAUTH_RUNTIME_NETWORK_CIDR` и прокидывают trusted network boundary в `api`, чтобы runtime не зависел от случайного Docker subnet-а.
- Verification после fix подтверждена: `backend/OtpAuth.Infrastructure.Tests` проходят `304/304`, packaging contract checks проходят, а regression coverage теперь отдельно фиксирует trusted `ForwardedHeadersMiddleware` path плюс existing production secure-cookie API tests.
- На `ghostring` подтвержден live pilot runtime: `bootstrap ensure-database/migrate/upsert-admin-user` прошли, `redis/api/admin/worker` healthy, `csrf-token` возвращает `200`, а `operator` успешно проходит `login -> session` через `https://admin.ghostring.ru:18443/`.
- Добавлен отдельный operational note `OTP/Delivery/Ghostring Runtime Runbook.md` с текущим public endpoint, smoke-checks, runtime boundary и residuals; следующий practical step смещен с infra rollout на `ProjectManager` pilot integration против уже живого `DT-1520`.
- Применен reverse-proxy forwarding fix из актуального `origin/main`: `ghostring` runtime пересоздан на детерминированной сети `172.29.152.0/24`, `runtime.env` получил `OTPAUTH_RUNTIME_NETWORK_CIDR` и `ReverseProxy__Enabled=true`, а `ConnectionStrings__Postgres` переведен на host-side gateway `172.29.152.1`.
- Host `nginx` теперь публикует `admin.ghostring.ru` на `https://admin.ghostring.ru:18443/` с отдельным `Let's Encrypt` сертификатом для `admin.ghostring.ru`; public smoke-check `GET /health/api` проходит, `csrf-token` снова отдает `200`, а operator login `operator` через public hostname подтвержден end-to-end.
- Текущий pilot runtime operational status: `redis`, `api`, `admin`, `worker` healthy; residual blocker для первого operator login закрыт, но публичный ingress по-прежнему живет на `:18443`, потому что `:443` на `185.153.182.121` остается занят `ocserv`.
- Для `ProjectManager` создан отдельный integration client `otpauth-projectmanager` без переиспользования `otpauth-crm`: штатный path прошел через `OtpAuth.Migrations seed-bootstrap-clients` с одноразовым server-side env override и сохранением plaintext секрета только в `/opt/dt-1520-authenticator/secrets/projectmanager-client-secret.txt`; новый client ограничен scope `challenges.read/write`, token issuance через public `/oauth2/token` подтверждена на live runtime.
- Зафиксирована отдельная обязательная post-pilot задача `OTP/Delivery/Admin Client Management Follow-Up.md`: после первого ручного pilot-теста нужно добить `Admin UI` и backend admin contour до полноценного integration client onboarding/rotation lifecycle без ручных bootstrap workaround-ов.
- Зафиксирована отдельная follow-up/discovery задача `OTP/Delivery/QR Device Onboarding Follow-Up.md`: после обсуждения нужно оформить operator-ready QR-based device onboarding поверх existing `auth.device_activation_codes`, но базовый security contract должен оставаться server-side atomic consume + TTL, а не зависеть только от команды телефона "удалить ссылку".
- Итог обсуждения QR onboarding дополнительно зафиксирован в vault: первый рекомендуемый flow идет через `Admin UI -> one-time activation artifact -> QR -> existing /api/v1/devices/activate`, при этом для первого slice выбран `opaque activation payload`, а телефонная команда на удаление ссылки допускается только как secondary UX signal.
- На стороне `ProjectManager` закрыт первый implementation slice pilot MFA для create/update `VCS instance` credentials: write-path теперь идет через pending protected operation, backend-only вызов `DT-1520`, signed callback и idempotent apply вместо немедленного сохранения секрета.
- `ProjectManager` frontend получил pending approval UX, resume path через собственный backend и больше не держит `VCS` password в React state дольше submit flow; локальная verification (`dotnet test`, `npm test`, `npm run build`, solution/server/worker builds) и security review подтверждены без blocking findings.
- Remaining work после этого среза сместился с implementation на live wiring: заполнить `Authenticator` config в `ProjectManager`, синхронизировать `Security:SecretProtection` между server/worker, сверить фактические callback paths/header contract и затем провести первый ручной end-to-end pilot против живого runtime `ghostring`.
- Выполнена live Android emulator verification для ProjectManager pilot: на `emulator-5554` активировано устройство `077d09f9-8637-4583-8864-9b29ced707b4` для `externalUserId=f1d6afaa-8a5d-4fd3-9f75-0a5c0177df81`, основной `Push Approvals` UI увидел live pending challenge, `deny` подтвердил terminal `denied`, а `approve` через device PIN подтвердил terminal `approved` для challenge `a202ef93-c2f5-4645-80cf-06af37d1d86d`.
- По ходу emulator verification закрыты Android runtime gaps: добавлен debug-only activation helper, `BuildConfig.DEVICE_RUNTIME_BASE_URL` теперь задается через Gradle property `-PdeviceRuntimeBaseUrl`, `MainActivity` переведена на `FragmentActivity`, runtime HTTP calls перенесены на `Dispatchers.IO`, а `PushApprovalsRuntimeInteractor` больше не пересоздается из-за unstable fallback lambda keys.
- Первый ручной ProjectManager-created pilot подтвержден пользователем: вход проходит без `TOTP`/push challenge по принятому `ADR-034`, step-up authorization появляется на защищенной `VCS` operation и подтверждается через Android emulator; observed lag между запросом и появлением challenge на emulator составляет около `~60s`.
- Следующий practical hardening point: разобрать latency source в delivery path (`worker` timings, callback/delivery outbox, mobile foreground polling interval, текущий `PushDelivery:Provider=logging`) и решить, достаточно ли polling-only режима для pilot UX или нужен real push provider.
- Latency hardening оформлен как отдельный обязательный plan item: `OTP/Delivery/Push Delivery Latency Follow-Up.md`; `MVP Closure Iteration Plan` получил новый `Iteration 3 / Slice 3C` для observed `~60s` lag.
- Важно: offline verification не проводилась. Успешный pilot checkpoint относится только к online/live contour; поведение при потере сети, недоступности `DT-1520` runtime или offline fallback не считается подтвержденным.
- `2026-04-27`: после обсуждения post-pilot порядок изменен: `Admin Client Management -> QR Device Onboarding -> Official .NET SDK -> Reference Desktop + Backend stand -> latency/online TOTP fallback hardening`; `ProjectManager` больше не считается primary diagnostic contour для latency, а остается enterprise pilot proof.
- `2026-04-27`: принят `ADR-035` и добавлены delivery notes `Official Dotnet Integration SDK`, `Reference Desktop Backend Stand`, `Documentation Handoff Plan`; desktop default topology зафиксирован как `Desktop App -> Backend -> DT-1520`, без хранения integration `client_secret` в desktop app.
- `2026-04-27`: уточнен offline-code scope: `Android` генерирует `TOTP` локально, но reference backend и `DT-1520` остаются online, чтобы сохранить centralized verification, replay defense, audit и rate limiting.
- `2026-04-27`: для дальнейших code tasks зафиксировано правило documentation-as-DoD: вместе с кодом обновлять профильные vault notes, `Current State`, `Implementation Map`, ADR при необходимости, user/developer docs и session note.
- `2026-04-27`: пользователь создал repo roots `docs/`, `lib/` и `rdb_stand/`; vault синхронизирован с фактическими путями, а `OTP/Delivery/Documentation Session Starter Prompt.md` подготовлен как стартовый промт для отдельной Codex-сессии по документации, включая установку и настройку admin panel.
- `2026-04-27`: по уточнению пользователя первый documentation slice сделан как standalone `React + Vite` страница в `docs/` без явного backend: добавлены typed content data, UI sections, CSS modules, static hero asset, README, unit tests и Playwright desktop/mobile regression.
- `2026-04-27`: verification documentation app закрыта: `docs` проходит `npm test` (`3/3`), `npm run build` и `npm run test:e2e` (`2/2`); для Playwright пришлось установить локальный Chromium через `npx playwright install chromium`.
- `2026-04-27`: security review docs app закрыт: реальные secrets/tokens/signing material/private URLs with credentials/raw callback payloads/`pushToken` не добавлены, а `mobile/app/src/debug/PilotDeviceActivationActivity.kt` явно помечен как debug-only и не production onboarding.
- `2026-04-27`: по запросу на старт первого productization пункта добавлен `OTP/Delivery/Admin Client Management Iteration Plan.md` и `OTP/Delivery/Admin Client Management Context Reset Prompt.md`; track разбит на отдельные context-reset-friendly итерации с DoD, tests/security/docs gates и prompt-ом для следующей Codex-сессии.
- `2026-04-27`: закрыта `Admin Client Management / Iteration 0` как preflight/contract confirmation: прочитаны existing lifecycle stores/service, migration runner commands, admin permission/CSRF patterns и `Admin UI` feature layout; план уточнен под separate sanitized admin store, permissions `integration-clients.read|write`, tenant-bound lifecycle endpoints и server-generated one-time secrets.
- `2026-04-27`: закрыта `Admin Client Management / Iteration 1`: добавлены backend read model/store/handler и endpoint `GET /api/v1/admin/tenants/{tenantId}/integration-clients`, permission boundary `integration-clients.read`, bootstrap whitelist для `integration-clients.read|write`, docs/vault sync и tests для handler/mapper/API/permission whitelist; targeted backend tests проходят `10/10`, security review подтверждает отсутствие `client_secret_hash`/plaintext secret в read model.
- `2026-04-27`: закрыта `Admin Client Management / Iteration 2`: добавлены backend create contracts/handler/store path и endpoint `POST /api/v1/admin/integration-clients`; create требует admin cookie session, `integration-clients.write` и `CSRF`, принимает только whitelisted scopes, генерирует `clientSecret` server-side, хранит только hash, возвращает plaintext secret только в `201 Created` и пишет sanitized audit `admin_integration_client.created`. Первичный `dotnet test` был прерван после зависания; оставшиеся `dotnet` процессы остановлены, причина локализована как sandbox/file-lock `Access denied` на `backend/artifacts/obj`, после чего вне sandbox зелены targeted backend tests `19/19`, full `verify-backend.ps1` (`327/327` infra + `19/19` worker) и docs verification (`npm test`, `npm run build`, `npm run test:e2e`).
- `2026-04-27`: закрыта `Admin Client Management / Iteration 3`: backend получил tenant-bound lifecycle endpoints `rotate-secret`, `scopes`, `deactivate`, `reactivate` поверх separate sanitized admin integration client store. Rotate возвращает one-time server-generated `clientSecret`, scope/status changes обновляют `last_auth_state_changed_utc`, deactivate/reactivate используют stable `409` для already-target state, а audit пишет sanitized `admin_integration_client.secret_rotated|scopes_changed|deactivated|reactivated` без secret material. Targeted build внутри sandbox снова уперся в `Access denied` на `backend/artifacts/obj`; rerun вне sandbox прошел, targeted tests `AdminIntegrationClient*` зеленые `29/29`, full `verify-backend.ps1` зеленый (`343/343` infra + `19/19` worker), docs verification зеленая (`npm test`, `npm run build`, `npm run test:e2e`).
- `2026-04-27`: закрыта `Admin Client Management / Iteration 4`: `Admin UI` получил workspace `integration-clients` для tenant load/list, sanitized detail и create flow. Scope input ограничен checkbox whitelist `challenges:read|write`, `enrollments:write`, `devices:write`; generated `clientSecret` показывается только в one-time panel, хранится в текущем React state и очищается через discard/load/reset/reload. Verification зеленая: `admin npm test` (`36/36`), `npm run build`, `npm run test:e2e` (`5/5`).
- `2026-04-27`: закрыта `Admin Client Management / Iteration 5`: `Admin UI` получил lifecycle actions для selected integration client: rotate secret с one-time display/discard, update scopes, deactivate и reactivate. Lifecycle commands идут через shared admin API client с `CSRF`, используют selected `tenantId + clientId`, sensitive actions требуют explicit confirmation, inactive clients disable rotate/deactivate и открывают reactivate path. Security review закрыт: rotated secret не пишется в storage/read model/audit fixtures, scope input остается whitelist-only, stale/cross-tenant draft не используется для command route. Verification зеленая: `admin npm test` (`43/43`), `npm run build`, `npm run test:e2e` (`6/6`).
- `2026-04-27`: закрыта `Admin Client Management / Iteration 6`: полный backend verification зеленый через `backend/scripts/verify-backend.ps1` (`343/343` infra tests, `19/19` worker tests) после rerun вне sandbox из-за `Access denied` на `backend/artifacts/bin/*/.msCoverageSourceRootsMapping_*`; residual warning прежний и связан только с `IBM.Data.Db2` architecture mismatch в migrations.
- `2026-04-27`: полный `Admin UI` verification для closure зеленый: `npm test` (`43/43`), `npm run build`, `npm run test:e2e` (`6/6`). Документация обновлена в `docs/src/data/documentation.ts`, `docs/README.md`, `admin/README.md` и vault; `Admin Client Management Follow-Up` помечен completed, следующий continuation point — `QR Device Onboarding Follow-Up`.
- `2026-04-27`: начат `QR Device Onboarding Follow-Up`; трек разложен на итерации `backend/admin artifact contract -> Admin UI QR workspace -> Android QR scan/import -> e2e hardening/docs closure`.
- `2026-04-27`: закрыта `QR Device Onboarding / Iteration 1`: добавлены backend/admin endpoints для create/list/revoke one-time device onboarding artifacts, `revoked_utc` в `auth.device_activation_codes`, fail-closed activation rejection для revoked artifact и sanitized audit `admin_device_onboarding.created|revoked`; targeted backend tests `AdminDeviceOnboarding|DeviceApiTests` проходят (`26/26`), full backend verification зеленый (`354/354` infra + `19/19` worker), docs verification зеленая (`npm test`, `npm run build`, `npm run test:e2e`).
- `2026-04-27`: закрыта `QR Device Onboarding / Iteration 2`: `Admin UI` получил workspace `device-onboarding` для list/create/revoke one-time QR artifacts, `qrcode.react` используется для `QRCodeSVG`, activation payload показывается только в текущем UI state и очищается через discard/load/selection/revoke/reload. Security review закрыт: payload не пишется в browser storage, не возвращается list/detail path, create/revoke идут через CSRF-protected admin API client, QR не использует embedded remote images. Verification зеленая: `admin npm test` (`52/52`), `npm run build`, `npm run test:e2e` (`7/7`), `npm audit`; следующий continuation point — `QR Device Onboarding / Iteration 3` Android QR scan/import path.
- `2026-04-27`: закрыта `QR Device Onboarding / Iteration 3`: backend получил mobile-facing `POST /api/v1/devices/activate-onboarding` без integration bearer, который derives tenant/application/external user из server-side artifact и затем использует existing atomic activation/consume path; `mobile` получил `:feature:device-onboarding`, QR payload validation/workflow, `DeviceRuntimeSessionManager.activateWithOnboardingPayload` и real QR scanner через `CameraX + ML Kit Barcode Scanning`. Security review закрыт: Android не получает integration `client_secret`/bearer, QR payload не содержит trusted tenant/user claims, invalid/expired/revoked/consumed/wrong-platform artifacts fail closed, scanner не логирует payload. Verification зеленая: backend targeted `DeviceApiTests` (`19/19`), full `verify-backend.ps1` (`357/357` infra + `19/19` worker), Android unit `:feature:device-onboarding:testDebugUnitTest :app:testDebugUnitTest`, `:app:assembleDebug`, instrumented `DeviceOnboardingUiTest` (`2/2`) на `Pixel_10_Pro`, MCP smoke на `emulator-5554` подтвердил app launch, `Device onboarding`, `Scan QR` и camera scanner screen; следующий continuation point — `QR Device Onboarding / Iteration 4` end-to-end hardening and docs closure.
- `2026-04-27`: закрыта `QR Device Onboarding / Iteration 4`: full backend verification зеленая (`357/357` infra + `19/19` worker), `admin` зеленый (`npm test` `52/52`, `npm run build`, `npm run test:e2e` `7/7`), `docs` зеленые (`npm test`, `npm run build`, `npm run test:e2e`), Android зеленый (`:feature:device-onboarding:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug`, `:app:connectedDebugAndroidTest` `14/14`) и live MCP smoke подтвердил `MainActivity`, `Device onboarding`, `Scan QR` и manual payload fallback на `emulator-5554`. Исправлены closure-дефекты: Playwright QR e2e spec больше не ломает production TypeScript build из-за DOM-типа, а `AuthenticatorAppUiTest` стабилизирован под новую scroll layout. `QR Device Onboarding Follow-Up` помечен completed; следующий continuation point — `Official .NET SDK`.
- `2026-04-27`: построен iteration plan для `Official .NET SDK` в `OTP/Delivery/Official Dotnet Integration SDK.md`: `Iteration 0` contract preflight, `Iteration 1` `lib/` scaffold, `Iteration 2` core HTTP/token/problem foundation, `Iteration 3` challenges/TOTP typed client, `Iteration 4` device/push helpers, `Iteration 5` callback signature validation, `Iteration 6` ASP.NET Core helpers, `Iteration 7` desktop session helpers, `Iteration 8` package docs/sample backend flow и `Iteration 9` prerelease closure/handoff к reference stand. Перед планированием сверена актуальная `.NET` guidance через Context7 `/dotnet/docs`.
- `2026-04-27`: по просьбе пользователя зафиксирован проектный workaround для частого зависания тестов: в `AGENTS.md`, `backend/README.md`, `mobile/README.md` и `Implementation Map` добавлены инструкции проверять stale `dotnet/MSBuild/VBCSCompiler` и `java/gradle` процессы, останавливать только процессы сорванного прогона, backend повторять через sequential verify или `dotnet test` с disabled parallel build, Android запускать с `JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'`, а sandbox-only `Access denied` повторять тем же command вне sandbox с approval.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 0`: через Context7 `/dotnet/docs` и `/nuget/docs.microsoft.com-nuget` сверены `.NET`/NuGet guidance, в `OTP/Delivery/Official Dotnet Integration SDK.md` зафиксированы package ids, `net8.0` baseline, no-default `netstandard2.0/net10.0`, result/error-oriented API style, initial public concepts, package metadata/README/XML docs policy и security non-goals. Добавлен `OTP/Delivery/Official Dotnet Integration SDK Context Reset Prompt.md`; code scaffold в `lib/` намеренно отложен до `Iteration 1`.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 1`: создан `lib/Dt1520.Authenticator.slnx`, package projects `Dt1520.Authenticator.Client|AspNetCore|Desktop`, matching xUnit test projects, shared package props/targets, package README/XML documentation inclusion and `lib/README.md`. Security review закрыт: package metadata/README не содержат real secrets/tokens/private credential URLs/raw callback payloads/mobile device tokens, desktop package сохраняет `Desktop App -> Integrator Backend -> DT-1520 Authenticator` и не вводит secret-bearing direct client config. Verification зеленая: `dotnet restore`, sequential `dotnet build`, `dotnet test` (`6/6`), Release build, Release `dotnet pack`; package inspection подтвердил `README.md` и `lib/net8.0/*.xml` внутри `.nupkg`. Initial sandbox attempts hit `Access denied` in `lib/artifacts/obj`, stale `dotnet/VBCSCompiler` processes from failed run were stopped and generated `lib/artifacts` was cleaned before rerun outside sandbox.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 2`: `Dt1520.Authenticator.Client` получил core HTTP/token/problem foundation: options/credentials validation, OAuth `client_credentials` token request to `/oauth2/token`, token cache with expiry skew, base-URL-scoped authorized JSON plumbing, stable `ProblemDetails` mapping and separate cancellation/timeout/transport errors. Security review закрыт: `client_secret` и bearer token redacted in `ToString`, validation/errors не echo secret material, `Authorization` не attach-ится outside configured base URL, raw response bodies не сохраняются в SDK errors. Verification зеленая: `dotnet build` Debug, `dotnet test` Debug (`24/24`), Release build, Release test (`24/24`) и Release `dotnet pack`; следующий continuation point — `Official .NET SDK / Iteration 3` challenges and online `TOTP` typed client.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 3`: `Dt1520.Authenticator.Client` получил `CreateChallengeAsync`, `GetChallengeAsync`, `VerifyTotpAsync`, public challenge models, snake_case enum JSON mapping, `Idempotency-Key` header support, validation-before-network and `TOTP` code redaction. Security review закрыт: bearer auth остается scoped to configured base URL, request `ToString()` не раскрывает code/callback/user subject, mobile/device secrets не добавлены. Verification зеленая: Debug/Release build/test (`34/34`) и Release pack; следующий continuation point — `Official .NET SDK / Iteration 4` device lookup and push workflow helpers.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 4`: `Dt1520.Authenticator.Client` получил typed `GET /api/v1/devices` lookup, `SelectSinglePushDeviceAsync`, safe device routing models, no-device/ambiguous selection outcomes и `PushChallengeOutcome` для pending/terminal push states. Security review закрыт: SDK не раскрывает `pushToken`, `publicKey`, `installationId`, device access/refresh tokens, QR activation payloads или desktop secret-bearing config; helper не принимает local trust decisions, а выбирает только среди backend-returned active push-capable candidates. Verification зеленая: Debug build, Debug tests (`47/47`), Release build, Release tests (`47/47`) и Release pack; следующий continuation point — `Official .NET SDK / Iteration 5` callback signature validation.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 5`: `Dt1520.Authenticator.Client` получил framework-agnostic `CallbackSignatureVerifier` для `X-OTPAuth-Signature` формата `sha256=<hex>`, HMAC-SHA256 over original raw request body bytes, timing-safe compare, typed failure kinds и optional timestamp tolerance. Security review закрыт: verifier не парсит/ресериализует JSON, не пишет signing secret/raw payload в result/ToString, unsupported algorithms fail closed, ASP.NET Core dependency и desktop secret-bearing config не добавлены. Verification зеленая: Debug build/test (`60/60`), Release build/test (`60/60`) и Release pack; следующий continuation point — `Official .NET SDK / Iteration 6` ASP.NET Core DI/options/callback helper package.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 6`: `Dt1520.Authenticator.AspNetCore` получил `AddDt1520Authenticator(...)`, backend-hosted options binding/validation, named `IHttpClientFactory` registration для `Dt1520AuthenticatorClient`, raw-body `HttpRequest` callback validator через `EnableBuffering()` с body rewind и sanitized failure results/logging. Security review закрыт: `ClientSecret`/`CallbackSigningSecret` остаются server-side options, raw callback payload не логируется/не сохраняется, business commit helper не добавлен, desktop/browser direct secret path не появился. Verification зеленая: `dotnet restore`, Debug build/test (`70/70`), Release build/test (`70/70`), Release `dotnet pack`; следующий continuation point — `Official .NET SDK / Iteration 7` desktop session and polling helper package.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 7`: `Dt1520.Authenticator.Desktop` получил desktop-safe approval session/status/outcome models и `DesktopApprovalPoller`, который опрашивает только integrator backend base URL + relative polling path через caller-provided `HttpClient`. Security review закрыт: desktop package не имеет `client_secret`, bearer token, callback signing secret, direct DT-1520 base URL, dependency on `Dt1520.Authenticator.Client`, raw backend failure body exposure или business commit helper. Verification зеленая: Debug build/test (`82/82`, desktop `14/14`), Release build/test (`82/82`), Release `dotnet pack`, docs `npm test`, `npm run build`, `npm run test:e2e`; следующий continuation point — `Official .NET SDK / Iteration 8` package docs/sample backend flow.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 8`: package documentation доведена до usable handoff — обновлены `lib/README.md`, package README files, добавлен `lib/samples/aspnetcore-protected-operation/README.md`, docs app получила отдельную `.NET SDK` section. Security review закрыт: runtime SDK code не менялся, `clientSecret`/`callbackSigningSecret` остаются backend-only, sample commits protected change only after `approved`, callback validation uses original body bytes, secrets/tokens/raw payloads/QR payloads/mobile tokens не добавлены. Verification зеленая: SDK Debug build/test (`84/84`), Release build/test (`84/84`), Release `dotnet pack`, `.nupkg` inspection подтвердил `README.md` + XML docs, docs `npm test` (`3/3`), `npm run build`, `npm run test:e2e` (`2/2`); следующий continuation point — `Official .NET SDK / Iteration 9` prerelease closure/handoff к `Reference Desktop Backend Stand`.
- `2026-04-27`: закрыта `Official .NET SDK / Iteration 9`: prerelease handoff оформлен в `lib/PRERELEASE-HANDOFF.md`, стартовый contract следующего трека добавлен в `rdb_stand/README.md`, а `PrereleaseHandoffTests` покрывают package gate, reference stand API list и desktop/backend secret boundary. Security review закрыт: desktop/browser direct-to-DT-1520 path не появился, secrets остаются backend-only, callback validation требует original raw body bytes, non-approved outcomes не коммитят business state, package README/XML/nuspec marker scan чистый; `Dt1520AuthenticatorAccessToken` XML-doc wording заменен с `Bearer token value` на `Access token value`, чтобы package inspection не ловил ложный secret marker. Verification зеленая: SDK Debug build/test (`86/86`), Release build/test (`86/86`), Release `dotnet pack`, direct pack three package projects, `.nupkg` inspection подтвердил README/XML/version/license, docs `npm test` (`3/3`), `npm run build`, `npm run test:e2e` (`2/2`); следующий continuation point — `Reference Desktop Backend Stand`.
- `2026-04-27`: закрыта `Reference Desktop Backend Stand / Iteration 1`: создан runnable `.NET` scaffold в `rdb_stand/` с ASP.NET Core reference backend, console desktop shell, SDK project references, signed callback validation over raw body, status polling shape, exact-once approved commit flag and online `TOTP` fallback. Security review закрыт: desktop хранит только backend URL, backend owns DT-1520 secrets, callback payload не логируется, TOTP code не сохраняется, non-approved outcomes не коммитят. Verification зеленая: `dotnet restore`, `dotnet build` вне sandbox после `Access denied` на `rdb_stand/artifacts/obj`, `dotnet test` (`4/4`); следующий continuation point — live runtime wiring against real DT-1520 + QR-activated Android device and latency measurement.
- `2026-04-27`: закрыта `Reference Desktop Backend Stand / Iteration 2`: добавлен local live wiring handoff без реальных секретов — ignored local appsettings, `appsettings.Development.example.json`, env-var runbook, sanitized `GET /api/reference/live-readiness` и fail-fast validation callback URL под DT-1520 create challenge rules (`HTTPS`, no localhost/private IP, no embedded credentials). Security review закрыт: readiness не раскрывает secret material, desktop по-прежнему хранит только backend URL, real secrets остаются env/local ignored config. Verification зеленая: `dotnet restore`, `dotnet build` вне sandbox из-за `Access denied` на `rdb_stand/artifacts/obj`, `dotnet test` (`11/11`); следующий continuation point — выполнить live runbook с реальным DT-1520 runtime и QR-activated Android device.
- `2026-04-28`: выполнен следующий live execution preflight для `Reference Desktop Backend Stand`: Android MCP видит `emulator-5554`, приложение `ru.dt1520.security.authenticator` запускается в `MainActivity`, UI node `Device onboarding` найден. Live env/local config отсутствует полностью, поэтому реальный push/TOTP flow не стартовал. Добавлен `--preflight` в `ReferenceBackend`, который выводит sanitized readiness до старта web host и не раскрывает `clientSecret`/callback signing secret; локальный запуск `dotnet run --no-build --project .\src\ReferenceBackend\ReferenceBackend.csproj -- --preflight` корректно вернул not ready с missing config issues. Verification зеленая: build вне sandbox, `dotnet test` (`14/14`), docs `npm test/build/e2e`; следующий unblock — заполнить backend-only DT-1520 config и внешний HTTPS callback URL.
- `2026-04-28`: исправлен `NU1105` сценарий из Visual Studio для `rdb_stand`: stand solution теперь включает SDK projects из `../lib/src`, а не только stand projects, поэтому Visual Studio restore/rebuild видит `ProjectReference` graph целиком. Сверено с NuGet docs по `NU1105`: ошибка возникает, когда NuGet не может прочитать project information, в Visual Studio часто из-за unloaded referenced projects; для текущего workspace корректный fix — добавить referenced SDK projects в solution.
- `2026-04-28`: добавлен `DesktopWpfTest` для MVP-наглядности reference stand: WPF MVVM shell сохраняет demo/live wiring form values в encrypted JSON под LocalAppData, запускает approval/status/online TOTP fallback только через reference backend и не сохраняет TOTP-код. Security review закрыт как demo-only local storage, не production secret store; verification зеленая: `dotnet restore`, `dotnet build --no-restore -maxcpucount:1`, `dotnet test --no-build -maxcpucount:1` (`DesktopWpfTest.Tests` `8/8`, `ReferenceBackend.Tests` `14/14`).
- `2026-04-28`: по итогам live QR activation attempt открыт planned follow-up [[../Delivery/QR Device Onboarding Runtime URL Follow-Up]]: текущий Android QR onboarding всё ещё зависит от APK build flag `-PdeviceRuntimeBaseUrl`, а целевой contract должен передавать public `runtimeBaseUrl` в QR envelope вместе с one-time `activationPayload`. План сохранен итерациями: Admin QR envelope, Android parser, activation через QR runtime URL, encrypted persistence runtime URL with device session, end-to-end verification/docs.
- `2026-04-28`: в QR runtime follow-up добавлена следующая post-QR итерация `Callback URL Policy Configuration`: после переноса runtime URL в QR нужно вынести строгие callback URL правила из hardcoded public-Internet assumptions в настраиваемую policy для `PublicInternet`, `PrivateNetwork` и `LocalDevelopment`, с admin/runtime configuration, tests и security review.
- `2026-04-28`: в тот же follow-up добавлен post-QR admin rework как итерации `Tenant-Centric Admin Contracts`, `Admin Tenant Directory Page`, `Tenant Management Page` и `Admin Reports, Metrics And Cleanup`: цель — заменить набор MVP-блоков tenant-centric workflow, где operator создает tenant/API-client, открывает управление tenant/client, работает с пользователями, устройствами, callback/runtime policy и отчетами без ручного копирования ID между блоками.
- `2026-04-28`: QR runtime/admin follow-up перепланирован под lightweight stage gates: в итерациях 1-10 оставлены только минимально необходимые targeted tests/checks, а полный backend/admin/mobile/Android MCP/rdb_stand/docs/live QR verification вынесен в отдельный `Final Integrated Verification Gate` после Iteration 10.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 1`: Admin UI QR value теперь является JSON envelope `{ v, runtimeBaseUrl, activationPayload }`, runtime URL выводится из `VITE_ADMIN_API_BASE_URL` или текущего public origin, а backend contract сохранен как one-time `dac_...` activation payload without read-back. Добавлены `deviceOnboardingQrEnvelope` model/tests, обновлены `DeviceOnboardingQrPanel`, component tests и targeted e2e fixture/spec; security review подтвердил, что QR не несет trusted tenant/user/application claims и payload/envelope не сохраняются в browser storage. Verification: targeted `admin npm test -- --run ...` `11/11`, `npm run build`, targeted `npm run test:e2e -- admin-device-onboarding.spec.ts` `1/1`; первые sandbox-прогоны Vitest/Playwright уперлись в `EPERM`, идентичные команды прошли вне sandbox.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 2`: Android parser в `:feature:device-onboarding` теперь принимает v1 JSON envelope и legacy raw `dac_...`; `DeviceOnboardingPayload` отдает `activationPayload`, nullable `runtimeBaseUrl` и compatibility alias `value`, а app activation wiring не менялся. Runtime URL validation fail-closed: только `https`, non-empty host и no embedded credentials/userinfo; ошибки generic и не echo-ят credential-bearing URL. Android MCP подтвердил `emulator-5554`; verification `:feature:device-onboarding:testDebugUnitTest` зеленая после rerun вне sandbox с `JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'`, первичный sandbox-прогон уперся в `%USERPROFILE%\.gradle` lock `Access denied`.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 3`: Android activation теперь выбирает runtime из QR `runtimeBaseUrl` при наличии, а legacy raw `dac_...` временно использует `BuildConfig.DEVICE_RUNTIME_BASE_URL` fallback; legacy без runtime URL получает sanitized сообщение, что QR не содержит runtime адрес. Добавлен `DeviceOnboardingRuntimeResolver` и unit tests для precedence/missing-runtime behavior. Security review закрыт: QR runtime URL не считается trusted tenant/user/application claim, activation отправляет только one-time payload, credential-bearing URL не логируются и runtime URL еще не persisted. Verification зеленая: `:feature:device-onboarding:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug`; fresh `app-debug.apk` установлен и запущен на `emulator-5554`, MCP screenshot подтвердил `Device onboarding` UI; docs `npm test` и `npm run build` зеленые после handoff text sync.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 4`: Android `StoredDeviceSession` теперь хранит optional encrypted `runtimeBaseUrl`, legacy session snapshots без URL читаются как `runtimeBaseUrl = null`, `DeviceRuntimeSessionManager` записывает runtime URL на activation и сохраняет его на refresh, а `AuthenticatorApp` после restart использует persisted URL, если `BuildConfig.DEVICE_RUNTIME_BASE_URL` пустой. Security review закрыт: runtime URL остается public routing metadata, credential-bearing URL rejects before storage, QR activation payload/device tokens не смешиваются с URL. Verification зеленая: `:security:storage:testDebugUnitTest :app:testDebugUnitTest`, `:feature:device-onboarding:testDebugUnitTest :app:assembleDebug` без `-PdeviceRuntimeBaseUrl`; оба sandbox-прогона уперлись в `%USERPROFILE%\.gradle` lock `Access denied` и были повторены вне sandbox с Android Studio JBR. Fresh `app-debug.apk` установлен на `emulator-5554`, direct `am start` открыл `MainActivity`, screenshot подтвердил `Device onboarding` UI.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 5`: выполнен documentation handoff без runtime-code изменений. Vault/docs/mobile handoff теперь фиксируют целевой contract `Admin QR envelope -> Android activation via QR runtimeBaseUrl -> encrypted device-session runtime URL reuse`; `-PdeviceRuntimeBaseUrl` оставлен только как legacy/debug fallback для raw `dac_...`. Security review: новые секреты/claims/storage paths не добавлялись, QR не становится trusted tenant/user/application source, full live proof намеренно перенесен на `Final Integrated Verification Gate` после Iteration 10. Verification: `docs npm test` и `docs npm run build` зеленые.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 6`: callback URL strictness вынесена в explicit policy model `PublicInternet|PrivateNetwork|LocalDevelopment`. Backend `CreateChallenge` использует configured `ChallengeCallbackUrlPolicy`, production default остается strict, private/local relaxations opt-in, validation errors не echo-ят rejected URL. Admin read-only `/api/v1/admin/runtime-configuration` показывает active policy без callback URLs/secret material. `rdb_stand` получил matching `ReferenceBackend__CallbackUrlPolicyMode` и `ReferenceBackend__AllowInsecureCallbackHttp`, а readiness/preflight показывают active policy. Security review закрыт: embedded credentials/userinfo, fragments и root-only paths reject-ятся, secret material не выводится. Verification зеленая: backend targeted tests `31/31`; reference targeted tests `13/13`. Во время проверки остановлены только stale процессы последнего зависшего прогона (`dotnet/MSBuild` и `ReferenceBackend 35412`), sandbox `Access denied` повторы выполнены вне sandbox.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 7`: backend получил tenant-centric admin contracts для будущей Admin UI directory page: `GET /api/v1/admin/tenants`, `GET /api/v1/admin/tenants/{tenantId}/directory`, `POST /api/v1/admin/tenants`, `POST /api/v1/admin/tenants/quick-create`, permissions `tenants.read|write`, migration `auth.tenants/auth.tenant_applications`, server-generated quick-create `tenant + application + integration client + one-time clientSecret` и sanitized audit `admin_tenant.created|quick_created`. Security review закрыт: read models не возвращают `client_secret_hash`, quick-create rejects operator-provided secrets, audit не пишет secret/hash, write endpoints требуют CSRF. Verification зеленая: targeted backend tests `17/17`; migration project build проходит вне sandbox после `Access denied`, с прежним known `IBM.Data.Db2` architecture warning; docs `npm test` (`3/3`) и `npm run build` зеленые.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 8`: `Admin UI` получил primary tenant directory workspace `admin/src/features/tenant-directory` поверх typed admin API client methods `listTenants/getTenantDirectory/createTenant/quickCreateTenant`. Operator может загрузить tenants, открыть directory detail, выполнить quick create `tenant + application + initial API client` с one-time `clientSecret`, а также advanced/manual tenant create with status. Security review закрыт: generated secret хранится только в текущем React state, discard/reload очищают его, storage non-persistence покрыта Playwright, list/detail не показывают `client_secret_hash`; status mutation backend endpoints не добавлялись и оставлены как later tenant management/status command slice. Verification зеленая: targeted `admin npm test -- --run src/features/tenant-directory/TenantDirectoryPanels.test.tsx src/shared/api/admin-api.test.ts` (`19/19`) после sandbox `spawn EPERM` rerun вне sandbox, `npm run build`, targeted `npm run test:e2e -- admin-tenants.spec.ts` (`3/3`) после sandbox `test-results/.last-run.json` `EPERM` rerun вне sandbox.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 9`: добавлен selected-tenant management workspace `admin/src/features/tenant-management` с tabs `Overview/API clients/Users & devices/Runtime/Reports`. Actions теперь берут tenant/application/client/user context из selected directory state: API client create/rotate/scopes/deactivate/reactivate, selected-user devices/revoke, QR issue, runtime policy read и reports работают без ручного копирования IDs между блоками. Security review закрыт: admin commands reuse cookie session + CSRF, one-time client secrets и QR activation payloads остаются только в текущем React state, reports/runtime не показывают `client_secret`, secret hashes, callback signing secrets, raw callback payloads, push tokens или activation payload persistence. Verification зеленая: targeted tenant-management Vitest `3/3`, shared API client Vitest `16/16`, full admin `npm test` `67/67`, `npm run build`, targeted Playwright `admin-tenants.spec.ts` `4/4`; docs `npm test` `3/3`, `npm run build`, `npm run test:e2e` `2/2`; initial sandbox Vitest runs hit `spawn EPERM` and were repeated outside sandbox.
- `2026-04-28`: закрыта `QR Device Onboarding Runtime URL Follow-Up / Iteration 10`: Admin UI landing стал tenant-centric для sessions с `tenants.*`, старые copy-paste workspaces переведены в fallback-only path для sessions без tenant permissions. Reports tab теперь строит MVP snapshot из existing safe read models: deliveries, selected-user devices и QR onboarding artifacts; summary показывает delivery health, callback/webhook split, QR status counts и last approval/device activity markers без secret-bearing fields. Legacy e2e specs переведены на fixture session без tenant permissions, чтобы fallback оставался покрыт, но не был primary path. Security review закрыт: новых backend endpoints не добавлено, `clientSecret`, secret hashes, callback signing secrets, raw callback payloads, push tokens и QR activation payloads не попадают в reports/read paths/storage. Verification зеленая: targeted admin Vitest `19/19` после sandbox `spawn EPERM` rerun вне sandbox, full `admin npm test` `67/67`, `npm run build`, targeted `admin-tenants.spec.ts` `4/4`, full `npm run test:e2e` `11/11`, docs `npm test/build/test:e2e`; следующий continuation point — `Final Integrated Verification Gate`.
- `2026-04-28`: начат `Final Integrated Verification Gate`: backend/admin/docs/rdb_stand/mobile automated verification была зеленой, Android MCP подтвердил свежий APK на `emulator-5554`, но live ReferenceBackend operation вернул `502`. Разбор разделил проблему на три слоя: прямой .NET/Windows SChannel transport к `https://admin.ghostring.ru:18443` падает до HTTP response (`SEC_E_NO_CREDENTIALS`), Node/OpenSSL подтверждает reachable `/health/api` и `/oauth2/token`; current local config запрашивает forbidden `devices:read`; deployed token endpoint отвечает camelCase `accessToken/tokenType/expiresIn`, из-за чего SDK считает 200 token response invalid. Кодовый contract mismatch исправлен в backend: `IssueIntegrationTokenResponse` сериализуется как OAuth snake_case, добавлен regression test, `backend/scripts/verify-backend.ps1` зеленый (`388/388` infra + `19/19` worker). Live proof остается blocked до redeploy backend fix, scope alignment (`challenges:read challenges:write` или разрешить `devices:read`) и решения Windows SChannel path или запуска ReferenceBackend из OpenSSL/Linux contour.
- `2026-04-28`: после server redeploy `b91f590` локальный ReferenceBackend config выровнен на `Dt1520Authenticator:Scope=challenges:read challenges:write`; `--preflight` снова ready. Прямой Windows SChannel к `admin.ghostring.ru:18443` по-прежнему падает с `SEC_E_NO_CREDENTIALS`, поэтому outbound DT-1520 проверка выполнена через loopback-only Node/OpenSSL diagnostic proxy. `POST /api/reference/operations` теперь успешно возвращает accepted `Waiting` session, подтверждая redeployed snake_case token response и scope alignment. Android MCP на `emulator-5554` после запуска приложения все еще показывает empty pending inbox, а локальный tunnel для callback URL не запущен (`cloudflared/ngrok` отсутствуют в PATH); следующий live blocker — восстановить public callback tunnel и/или повторно активировать Android через QR runtime URL, затем проверить pending push и approve/deny/TOTP terminal flow.
- `2026-04-28`: принято уточнение безопасности/ownership: third-party quick tunnels (`trycloudflare`/аналогичные) не использовать для целевого live contour. Вместо этого подготовлен server-owned `ghostring` ReferenceBackend contour: добавлены `infra/docker/reference-backend.Dockerfile`, service `reference-backend` в `infra/docker-compose.ghostring.yml`, host nginx template `infra/nginx/reference-backend.ghostring.ru.conf.example` for `https://admin.ghostring.ru:18444/ -> 127.0.0.1:15188`, and `REFERENCE_BACKEND_*` env contract. ReferenceBackend внутри compose ходит к DT-1520 по private `http://api:8080/`, callback URL `https://admin.ghostring.ru:18444/api/reference/callbacks/dt1520`, desktop/WPF base URL `https://admin.ghostring.ru:18444/`. Verification: packaging contract checks passed, `docker compose --env-file infra/env/ghostring.runtime.env.example -f infra/docker-compose.ghostring.yml config --quiet` passed, Android `:app:assembleDebug` без `-PdeviceRuntimeBaseUrl` passed after sandbox Gradle lock rerun outside sandbox, fresh APK installed/launched on `emulator-5554` and MCP showed pending section + Device onboarding UI.
- `2026-04-29`: зафиксирована рекомендация по коробочному направлению: optional boxed `Integration Gateway` принят как следующий productization target для reference backend после server-owned contour, без зависимости от third-party tunnels. Одновременно зафиксировано обязательное future constraint: `TOTP` при `RDP` относится к отдельному access connector track (`RADIUS/NPS` с `TOTP-first` как вероятный первый production option), и дальнейшие решения по gateway/SDK/challenge/session/policy/audit должны сохранять reusable lower-level services, чтобы не переписывать код перед production access scenarios.
